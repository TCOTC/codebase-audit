---
name: codebase-audit
description: 'Audit and repair the SiYuan codebase (Go kernel, TypeScript frontend, Electron host) for hidden design, contract, and correctness problems that tests and linters cannot catch, then make the minimal safe fix. Covers duplicated single-source-of-truth literals, same-semantics implementations that drifted apart, state lost across async or lifecycle transitions, enumeration sets missing new members, unescaped interpolation into HTML, contract and format-compatibility gaps, and unverified cross-repo or cross-platform assumptions. Use when hunting latent design flaws or design drift, finding duplicated hardcoded paths or constants, auditing test blind spots, repairing a defect found by an audit, deciding whether a fix should stay local or become an architectural change, or preparing a large refactor. NOT for style, lint, runtime debugging, or security review.'
argument-hint: '[scope: package or directory] [focus: literals | drift | state | contract | all] [mode: audit | repair | arch | verify]'
---

# 代码库审计（隐藏问题扫描）

> **当前调校目标**：本 skill 的判据与实例围绕 SiYuan 仓库（`kernel/` Go + `app/src/` TypeScript）提炼。
> 用于其他仓库时，判据 A–H 与模式库仍然适用，但 [已知误报](./references/known-false-positives.md)
> 表、路径示例、`AGENTS.md` 约束需按目标仓库重新校准。校准结论回写 [实证数据](./references/evidence.md)。

## 适用场景

找出**测试和 linter 抓不到**的问题。这类问题的共同特征是「单侧看着都对，只有交叉或跨状态才暴露」，所以加单测无用。

**不适用**：代码风格（用 lint 工具）、运行时调试、安全审计（那是另一套方法论）。

## 工作模式

| 模式 | 做什么 | 是否改动文件 | 流程 |
|---|---|---|---|
| **审计**（默认） | 找缺陷，产出报告 | 否，只读 | 下方「执行流程」1–9 步 |
| **差分审查** | 审一个 PR / commit / diff | 否，只读 | [差分审查模式](./references/diff-review.md) |
| **修复** | 改代码修掉已确认的缺陷 | 是，**需用户明确授权** | 审计结论 → [修复手册](./references/repair-playbook.md) |
| **架构调整** | 改结构，消除缺陷的复发条件 | 是，**需用户明确授权** | 满足「该改架构」判据后按修复手册第 4 节 |
| **验证** | 复核既有发现的处理结果 | 否 | 「执行流程」第 1 步的验证模式 |

**审计结论不是修改授权**。用户只要求「看看有没有问题」时，做完审计就收口；
进修复模式前必须由用户明确要求改代码。

**差分审查与审计的唯一区别是范围**（diff vs 目录），判据与挑战门完全复用。
它值得单列的理由：`evidence.md` 实测**58.5% 的 bug 提交只碰 1–2 个文件、中位数 31 行**，
小 diff 才是主流场景，而它有一个审计模式看不到的产出——**「漏改」**（同一次改动里对称的一侧没改）。

## 全栈层面导航

判据 A–I 按**推理形态**组织（怎么推理）；[全栈层面地图](./references/stack-map.md) 按**仓库层面**组织（去哪找）。
确定范围时先查层面地图取该层的权威源、既有校验器与取证陷阱，再回到本文判据清单取检查法。

| 组 | 层面 | 主要判据 |
|---|---|---|
| 内核（Go） | L1 数据与索引、L2 API 与契约、L3 属性视图、L4 同步/快照/加密、L5 CLI/MCP/server | A、D1、D3、D3c、D4、G、H |
| 前端（TS/Electron） | L6 编辑器内核、L7 UI 与配置、L8 Electron 宿主与打包、**L11 交互与状态** | C、D1e、D1g、D3c、E2、F、**I** |
| 横切 | L9 i18n 与文档、L10 CI/测试/发布 | A、D3c、G3、G4、H、I4 |

## 核心原则

### 1. 判据可验证

报告任何一条前，必须能回答：「用什么命令或文件行号证明它？」给不出证据的发现不要写进报告。

### 2. 三层漏斗，不要直接读代码

本仓库约 1282 个非测试源文件，逐个人工阅读不可行。必须逐层收缩：

```
全部文件  →  机械筛选（脚本/grep，产出候选）  →  语义判断（只对候选）
  1282              百量级候选                       top 20-30
```

**机械的事交给脚本，AI 只做语义判断。** 不要让 AI 做统计计数——既浪费又不可靠（本仓库曾有 agent 手写模拟依赖逻辑，漏掉三条规则差点得出反向结论）。

### 3. 机械判据与语义判据分开

| 类型 | 特征 | 产出 |
|---|---|---|
| 机械判据 | 可用脚本/grep 穷举，有明确阈值 | 候选清单，仍需人工确认 |
| 语义判据 | 判据是「是否语义同一件事」，grep 只能给候选 | 结论 + 证据 |

不要把机械判据包装成「AI 扫描」，也不要把语义判断降级成 grep 匹配。

### 4. 发现必须过挑战门

AI 审计最大的风险是合理化（rationalization）——把「代码可以更好」当成「这是 bug」。每条发现都要过 [挑战门](./references/challenge-gate.md)，尤其安全类、文档未定义行为类。

**两个方向都要防**：既防把噪声报成缺陷（假阳性），也防把真缺陷判成噪声（假阴性）——后者更隐蔽，且一旦写进「已知误报」就会长期过滤掉后续同类命中。因此**降级理由本身也要过门**：凡理由是一条技术断言（「浏览器会忽略它」「CI 跑不到」「上游不会这样分派」）的，必须再验该断言是否只对部分输入成立。

**挑战门审的不只是发现，还包括建议的修法**：建议形如「照抄另一处实现」时，先确认两处的上游消费方是否相同（否则可能把「成功的幂等重试」变成服务端故障）。

### 5. 结论必须落回业务表现

代码层的「变量写反了」「两侧逻辑不一致」**不是报告终点**。人类判断一条发现是否值得修，看的是「用户实际看到什么、应看到什么」。因此每条发现必须给出：

- **业务表现**：从用户可见入口走到该行为的具体路径 + 实际观察到的结果。要能写成「打开 A - B，就会看到 C」
- **预期表现**：应该是什么，且必须给出**权威依据**（项目文档、上游定义、既有正确实现、前端渲染契约、功能 issue 的意图）。给不出依据的「预期」只是审计者个人意见，按挑战门规则应驳回
- **可验证不变量**：一条能用输入/输出关系判定真假的等式或断言（如「各类型计数之和 = 总数」），供人工快速验证

写不出业务表现的发现，通常说明影响范围未查清，应降级为附录观察项而不是主报告条目。

### 6. 判据库要累积

每轮审计的产出除了问题清单，还包括**新增的有效判据**和**新发现的误报**，两者都写回 [模式库](./references/patterns.md) 与本文「已知误报」。这是「反复扫描」能越来越准的唯一机制，否则每次从零开始。

**去重与累积同等重要**：判据要累积，**发现不能重复**。扫描脚本是机械的，同一处缺陷会被反复命中；若不加去重直接成文，报告会变成「同一问题的第 N 次报告」，读者无法分辨哪些是本轮真实增量，报告整体失去可信度。**重复命中只允许追加证据，不得重复立论。**

## 判据清单

### A. 单一真源缺失（机械，脚本覆盖）

充当标识符的字符串字面量（路径、键名、路由）在多处重复。**重复本身不是问题，重复的是「本该有单一真源」的东西才是问题。**

**风险分级**——只看重复次数会把三类混为一谈：

| 情形 | 风险 | 例 |
|---|---|---|
| 受类型系统校验，写错则编译失败 | 低 | 前端 API 路由字面量，受 `app/src/types/api/index.d.ts` 约束 |
| 裸字符串，无任何校验 | **高** | 内核里的 `"filesys_status_check"`、`"storage/local.json"` |
| 同目录已有同义常量却未复用 | **高** | 已有 `boxDocMetaName` 常量，但 `conf.json` 在各处裸写 |

**发现方式**：

```bash
python "<skill-dir>/scripts/scan_duplicated_literals.py" \
  --root kernel --root app/src --out <工作区外临时路径>
python "<skill-dir>/scripts/scan_unescaped_html.py" \
  --root app/src --out <工作区外临时路径>
# 前端 DOM 契约（data-type）的闭合集合——权威源是 Lute 输出的 NodeType
python "<skill-dir>/scripts/scan_dom_type_literals.py" \
  --root app/src --kind node --out <工作区外临时路径>
# 改了脚本后跑一次自检（含「根不存在时不得假成功」与默认值一致性）
python "<skill-dir>/scripts/test_scan_scripts.py" <目标仓库根目录>
```

`<skill-dir>` 指本 SKILL.md 所在目录。`--root` 按需替换为目标仓库的源码目录；
缺省值 `kernel`/`app/src` 是本仓库布局，用于其他仓库时必须显式传。

按 P1（路径类）/ P2（文件名类）/ P3（标识符类）分段，按跨文件数降序。**只看头部**。
`--min-files` 默认 4（实测本仓库：4 → 274 条，2 → 1060 条，约 3.9 倍噪声），不必手动传。

**「0 条发现」不等于「没问题」**：两个脚本在扫描根不存在时以退出码 2 报错而不是静默输出 0，
看到非零退出要先修路径。零发现与没扫到文件在输出上无法区分，这是审计工具最容易骗到自己的地方
（也正是判据 D1m / 模式 P35 「假成功」的形态）。

### B. 语义漂移（语义判断）

同一语义存在两份实现，且已出现行为差异。**这是最高优先级**：两份规则必须人工同步维护，迟早失配。

**发现**：grep 疑似同义的函数名（`isIgnore`/`skippedDir`/`ignored`/`sanitize`），再用 subagent 并行比对。

**验证**：写临时测试，**两侧都调用真实代码**——不要模拟对侧逻辑。模拟本身会引入误差，且误差正好落在最需要验证的地方。

### C. 状态保持（语义判断，本仓库最高频）

本仓库 2499 条提交里 `preserve`/`keep`/`restore` 共 **344 次**，是最大的单一类别。典型形态：

- 异步回调覆盖用户此前的输入
- 重建 UI 时未回填原值（如写 `value: ""` 而非读取当前值）
- 生命周期切换（只读与编辑互切、标签页切换、键盘弹出）时状态丢失
- 并发更新互相覆盖

**发现**：找「重建 / 重置 / 重新渲染」的位置，检查是否从当前状态读值，而非写死初始值。

### D. 一致性与对称（半机械）

- **D1** 相邻或镜像代码块不对称：`upsert`/`remove`、`add`/`delete`、`save`/`load`、`encode`/`decode`。
  同一函数族的近同构分支（`switch` 的同名 `case`、按类型平铺的 `calc*`/`render*`/`is*`）要逐项 diff **判定条件**，而非只看分支体。
  **恒真守卫是强信号**：`if nil != x && nil != x.Field` 若 `x.Field` 在上游已被无条件归一化，条件永远成立，
  说明漏掉了本应有的语义判空。验证法：读该值的唯一构造路径，再与同函数内兄弟分支的判空条件横向对比
- **D1b 恒假守卫 → P13**：守卫假定的实参形态与**全部调用点**实际传入的不符，条件恒 false，其后逻辑沦为死代码。
  强信号是「守卫与函数体对入参形态的假设互相矛盾」。**恒真看生产者**（上游归一化），**恒假看调用者**（契约不符）
- **D1c 同一算子的多条实现路径量纲/格式漂移 → P18**：grep 目标改为**枚举常量名 + 其字符串字面量**，
  逐路径 diff **量纲**（是否 ×100）、**数值类型**（整数除法/float）、**格式标记**（是否 `Percent`）；
  权威侧优先取同一 `switch` 内的兄弟分支。反例优先取整数除法截断点（`1/200 == 0`），不依赖单位约定。
  改量纲的修复必须列出排序/筛选/嵌套汇总/导出等下游消费点
- **D1d 同族方法只有一处缺前置守卫 → P19**：`LoadAndDelete` 后**没有 `else`** 是最典型形态。
  关键是**调用方未做存在性预检**——上游常按**路径形态**分派，与存在性正交。
  **recover 中间件会把 panic 静默转成 2xx**；严重度看**panic 之前是否已产生副作用**。
  修法与定性两坑：不可「照抄兄弟路径」（普通 error 可能被映射为 500）；「同族都挂了该守卫」须先按**有无副作用**分组
- **D1e 同族多实现里唯一一处读错 DOM 属性 → P20**：唯一一处读了该元素上不存在的属性
  （典型：`contenteditable` 的 div 断言成 `HTMLInputElement` 后读 `.value`），`undefined` 被 `|| ""` 掩盖。
  判据是 grep 同一变量名在各读取点的取值方式并分组，找少数派；再确认**无写入方**（才是「恒空」）
- **D1f 同一行内同一表达式重复出现（漏改下标）→ P24**：同一行内同一复杂表达式出现 ≥2 次，
  同文件又有 `[0]`+`[1]` 成对惯例即几乎必为笔误。兄弟陷阱：`splice(indexOf(x), 1)` 未命中时删**最后一个**元素
- **D1g 同类宿主页面的配置不对称 → P25**：机械提取同类配置块逐项 diff（比**成员集合**而非值），
  再 grep 共用脚本的 `require`/全局依赖；远端/降级路径往往是唯一触发场景
- **D1h 词法路径校验在符号链接面前失效 → P28、P34**：守卫用纯词法 `Clean`+`IsSubPath`，同族用 realpath/逐级 `Lstat`/`os.Root`。
  **必须区分叶子是软链 vs 中间组件是软链**——后者会顺着链接操作目标（目录场景是递归删除目录外整棵树）。
  叠加**免鉴权前缀**时升级为「匿名可读」。修法两坑：不可复用按前缀判定的敏感目录黑名单，也不可一律禁软链；
  同一 handler 的**读文件分支**同样要加固
- **D1i 异步队列吞掉落盘错误使调用方假成功 → P29**：失败只记日志 + 推提示，**函数仍返回成功**。
  焦点是「基于返回 nil 推断落盘成功」且随后有**破坏性动作**的调用方
- **D1j 同族路径上「前置守卫」的位置漂移 → P30**：看共享函数把守卫放在第几句，再看同族调用方的**副作用**
  在调用它之前还是之后；若副作用夹在中间，则「被拒绝」不是 no-op 而是部分破坏
- **D1k 模板条件隐藏集合 ⊋ 运行时恢复集合 → P31**：模板的 `${cond ? "fn__none" : ""}` 为集合 A、
  各恢复函数的 `classList.remove("fn__none")` 为集合 B，`A \ B` 即候选。
  附加风险：`previousElementSibling` 在 `/// #if MOBILE` 双模板下语义不同，会连带隐藏跨端不存在的目标
- **D1l 同族复制粘贴读错绑定对象 → P32**：对**已知为对象**的持久化值读 `.length`/`[0]`。
  定性注意：恒假的是**单个子条件**，必须写明「守卫被削弱成什么」，不能写「守卫等于不存在」
- **D1m CLI/脚本入口的「假成功」→ P35**：失败只经 **WS 推送**上报，而 CLI 进程没有接收方。
  检查点是子命令调用的 model 函数**是否有返回值**。变体：dry-run 被写在「需要某个可选参数」的条件里；
  空结果写文件（`O_TRUNC`）截断为 0 字节且报成功
- **D1n 适配层新增的规范化标记改变了安全敏感的既有输入 → P38**：grep 绑定层的规范化标记
  （`trim`/`lowercase`/`normalize`/`rejectEmpty`），逐字段问「是否参与密码学派生或已落盘的等值匹配」。
  方向是关键（宽进 vs 收窄），取证须做**新旧实现对比**。
  **标记名的字面语义不可信**；**不要用迁移后写的文档证明「这是保留行为」**（迁移会把新行为追认进文档）
- **D2** 同一操作存在多个 API 表面（直接方法 vs 包装视图、构造器 vs setter、同步 vs 异步），边界行为分歧
- **D3 闭合集合漏项 → P14**：switch／白名单／枚举未包含上游新增成员。**上游定义即权威源**，逐项比对；
  调用方自行补偿不算修复。三个易错判定点：① 跨语言同名常量表同名即成对，但差异方向**可能相反**，
  须先判定权威侧，不可一律向成员多的一侧补；② 能力白名单（能否接收子块／是否容器块／是否可编辑）的
  权威源是上游类型方法，同包若另有委托上游的等价函数，两者自相矛盾即强证据；③ 用 `git log -S` 核对时间线，
  区分「漏项」与「有意排除」。**修法不是补齐上游全部成员**
- **D3c 跨契约边界的手写 DTO → P15、P27**：同一 `json` 键集合常在四处各有一份（运行时配置、持久化 struct、
  `apicontract` struct、前端类型），DTO 多由抄写而来并冻结；标准 `encoding/json` 无 `DisallowUnknownFields`，
  缺失成员被**静默丢弃**——P4 的一致性缺陷在此升级为数据丢失。三个要点：① 指针强转互转（`(*A)(b)`）时
  必须在**等价位**逐字段插入；② **同一契约内 struct 与 map 并存时优先怀疑 struct 型成员**；
  ③ 前端键集合是内核 struct 的子集时，权威侧是内核持久化 struct，缺失表现为「设置项静默无效」。
  附带形态：前端用「对象深比较」做保存守门（先比键数）会让守门**恒不成立**，把「显示错误」升级为「无谓落盘」
- **D3d 手写多语言文档的同步漂移 → P39**：同一事实的 N 份手写副本（`docs/X.md` + `X.<lang>.md`），
  没有生成步骤或一致性断言钉住。**多语言文档天然无法收敛为单一真源**，放松的是**文本**，
  不是**结构与语言无关标识符**。**前提：先确认该文档不是生成物**——
  实测 `apigen` 只写 `index.d.ts`/`schema.json`/petal，不写 `docs/API*.md`。
  两类比对**缺一不可**：结构指纹抓「少一整节」，语言无关标识符抓「节内漏字段」……
  两者各有反向盲区（实测 ja 靠结构抓到、zh-CN 只能靠标识符抓到）。用 `scan_doc_parity.py`。
  **归一化先行，否则假阳性淹没真差异**：占位符名与示例实参名可译（见 P39 的四条修法陷阱）
- **D4 归约方向反转 → P10、P23**：聚合为标量时写成 `element += accumulator`，累加器恒为初值。
  **发现**：grep 该标量在函数内的全部出现，无赋值即恒为初值；重构是此类缺陷的高产入口，
  用 `git log -L <range>:<file>` 对照重构前的正确实现。补充检查点：**有界集合的容量裁剪必须与消费端同一端**
  （先确认消费端从哪一端取值，再看裁剪端与它是否同端、以及裁剪相对写入的先后决定稳态长度是否 off-by-one）

**发现**：对可疑函数 grep 分派条件（`HasPrefix`/`HasSuffix`/`case`），检查成对分支条数与顺序是否一致。闭合集合可用脚本机械提取后与权威源 diff。

### E. 隐式假设（语义判断，风险最高）

- **E1** 对跨仓库依赖的内部行为做出假设（内核依赖 dejavu 的忽略语义即属此类，且升级无感知）
- **E2** 平台假设：Windows 隐藏属性、大小写敏感性、路径分隔符、**宿主环境提供的全局配置**
  （注册表、环境变量、语言/区域设置、文件系统换行符）。实测三类：
  ① **注册表覆盖库内置表**：Windows 上 `mime.TypeByExtension` 读 `HKEY_CLASSES_ROOT` 覆盖 Go 内置映射
  （`.jpg` 可为 `application/jpg`），使「按 MIME 前缀判安全」的逻辑结论随宿主变化——且方向可反转（可被伪造成白名单内的类型）；
  ② **换行符**：工作区文件实际是 CRLF，而测试按 `"\n"` 切片源码（`.gitattributes` 未锁 `.js` 的 `eol`）；
  ③ **临时目录归属**：夹具建在 `t.TempDir()`，而其真值取决于它是否命中硬编码的路径黑名单，
  于是同一测试在 Linux/macOS/CI 上结论不同（`TMPDIR`、且 `GOTMPDIR` 会架空用例级覆盖）。
  **检查法**：先找出「判定输入来自环境」（`mime.TypeByExtension`、`os.Getenv`、`os.TempDir`、`path.Dir(HomeDir)`），
  再把该环境值**人为改成另一个合法值**跑一次。结论改变即为候选，且要考虑「方向反转」（从拒绝变放行）。
- **E3** 匹配语义过宽或过窄：后缀匹配（`HasSuffix`）会误伤同形路径；前缀匹配缺边界检查（`startsWith("/api")` 会匹配到 `/api-docs`）
- **E4** 解析结构化值时用朴素字符串操作，而非按规范分词（逗号列表、大小写折叠、空白裁剪）

**验证 E3/E4**：构造一个「按规范合法但会被该实现误判」的输入，能构造出来即为候选缺陷。

**验证 E2 的注意点**：不要用「我这里是正常的」来排除。环境类缺陷的特征是**在某个平台上恒成立**，
所以「在别的机器上跑一次」不构成证据；要给出该环境值的**来源**（哪一行读它）与**两个取值下的结论差**。

### F. 插值转义（机械，脚本覆盖）

把动态值插入 HTML 时未转义。本仓库约定是 `escapeHtml` / `sanitizeKernelHTML`。

**发现方式**：

```bash
python "<skill-dir>/scripts/scan_unescaped_html.py" \
  --root app/src --out <工作区外临时路径>
python "<skill-dir>/scripts/test_scan_scripts.py" <目标仓库根目录>   # 改了过滤规则后必须跑
```

`<skill-dir>` 指本 SKILL.md 所在目录。

**脚本产出的是候选，不是结论**。优先看表达式来自用户数据、文档名、标签名的位置；CSS 值与数值索引属噪声。

**实测误报率与审法（2026-09-14 校准，785 文件 / 244 候选）**：

| 项 | 值 |
|---|---|
| 候选点 / 涉及文件 | 244 / 95 |
| 归一化后**不同表达形态** | **43 种** |
| 抽样核对的候选 | 100+ 条 |
| 其中判为真缺陷 | **0** |

**「条数」会吓人，「形态数」才是真实工作量**：把表达式里的标识符替换成 `X`
（如 `html`、`item.label`、`escapeAriaLabel(X)` 各算一种）后，244 条只剩 43 种，
且前 6 种形态覆盖了一半以上。**按形态逐类判定，不要逐条读。**

抽样确认的**稳定误报类**（已在脚本中过滤或可直接跳过）：仓库常量表（`Constants.*`，31 条）、
裸大写下划线常量（16）、三元两侧均为字面量（16）、拼接好的 HTML 变量但**上游已转义**（如 `html` 内部逐项 `escapeHtml`）、
数字与几何量（`length`/`count`/`clientWidth + 16`）、受控 i18n 文案、`Lute.*` 生成值、
以及**用户自己配置里的字符串**（如导出水印文本——功能本就以 HTML 为输入，属自注入而非安全边界）。

**真正需要人工判断的只有三类**，其余可直接跳：
① **裸变量**（`html`、`rowHTML`、`svg`）——要去读**变量的拼装处**是否逐项转义，而不是读这一行；
② **由 DOM/接口驱动的值**（`item.getAttribute("data-content")`、`response.data.*`、`target.dataset.*`）——要确认上游是否可信；
③ **跨模块返回值**（`genXxxHTML(...)`、`getColIconByType(...)`）——要读被调函数是否自行转义。

**过滤器要用「必须保持不安全」的用例锁住**：`scripts/test_scan_scripts.py` 固定了 15 个应放行形态与 8 个应保留形态，
后者更重要——某条规则的写法退化（如正则能匹配空串）会让**全部候选**被静默过滤掉，而报告看起来仍然正常。

### G. 契约缺口

- **G1** 两个实现之间没有任何测试要求它们结论一致 → 缺交叉断言
- **G2** 序列化语义假设：JSON 数字精度、时间字段是字符串还是数字、空值与缺失的区别
- **G3** 测试替身（stub/mock 白名单）与源码 import 图之间没有一致性断言。典型形态：测试用手写的模块白名单
  驱动模块加载（`assert.ok(sources[name], "unexpected module " + name)`），白名单只覆盖「当时」的传递依赖；
  之后给链路上任一模块**新增一个 import**，测试立刻失败但报错只说 `unexpected module X`，与业务无关且难以归因。
  **两种失败形态要分别认**：① 模块整个不在白名单 → `unexpected module X`；② 模块在表里但缺新导出 →
  `TypeError: (0 , m_1.f) is not a function`（桩对象**存在但过时**，比情形 ① 隐蔽）；③ DOM 替身缺新调用的方法 →
  处理函数中途抛错，表现为「后续断言全部落空」（如 `classList` 只实现 `toggle` 未实现 `remove`）。
  检查法：机械比对「白名单 ∪ 被测模块的传递依赖」与「实际 import 集合」。
  **扩大影响面的两个倍率**：ⓐ 同一份桩被多个测试文件复制（实测一个新增导出连挂 3 个测试文件）；
  ⓑ **测试不在 CI 里跑**时这些失败只在本地可见（见下条 `G4`）。
  **此项与本 skill 产出的缺陷无关，但会污染「修复验证」的回归结论**——验证轮遇到它必须先排除再判定回归。
- **G4** 测试存在但不在 CI 执行集内。白名单式 CI（`-run "Test.*Contract|..."` + 显式文件列表）会在
test 数量增长后迅速失真：本地全量一跑就红、CI 恒绿，于是**没人能区分新回归与存量噪声**。
  检查法：把 CI 配置里出现的包/文件集合与 `go list ./...`（或测试运行器的实际收集结果）取差集，非空即命中。
  **审计含义**：修完缺陷后不能只问「有没有补测试」，还要问「这条测试会不会被执行」——否则「已修复」只在本地成立。
  **补全后往往立刻暴雷，且以「三连提交」的形式**：把白名单改成全量后，测试的**环境前提**才第一次暴露。
  典型形态：夹具建在 `t.TempDir()`，而测试的前提是「该目录是否命中硬编码的系统目录黑名单」——
  两组测试的前提**互相矛盾**（一组要求 `TMPDIR` 处于 `/tmp` 这类黑名单前缀下，另一组恰好相反）。
  修复者只能先全局改 `TMPDIR`，再为要求相反的那个测试把 `TMPDIR` 改回去。
  检查法：grep 夹具代码里的 `TempDir`/`MkdirTemp` 与环境变量（`TMPDIR`），再 grep 路径黑名单常量，
  看是否有测试的断言真值**依赖两者前缀关系**。可靠修法是造显式路径（在工作空间内、或直接指向黑名单目录），
  而不是依赖环境的临时根目录。
  **「矛盾」的严格证明法：把环境变量往两个方向各拨一次，做成 2×2 表**。
  只需两次 CI 运行（或两次本地运行）就能证明「不存在任何一个取值能让两组同时通过」：

  | 取值 | A 组（要求命中黑名单） | B 组（要求不命中） |
  |---|---|---|
  | 默认（Linux `/tmp`、macOS `/var/folders/…`） | 通过 | **FAIL** |
  | 非黑名单前缀（如 CI 的 `runner.temp`） | **FAIL** | 通过 |

  因为 A 的路径就落在 `TMPDIR` 下，「A 通过」要求 `TMPDIR` 前缀命中黑名单，而这又必然使同一前缀下的
  B 组夹具被判为敏感——**两个要求在同一变量上不可同时满足**，这不是猜测而是由黑名单定义直接推出的。
  **判定「修复是否真的稳」要学会看这类「依赖环境默认值才成立」的修复**：
  ⓐ 只在特定平台上验过（`filepath.Separator == '/'` 守卫会把整个分支在 Windows 上跳过）；
  ⓑ 只在 CI 的特定取值下成立（开发机默认值与 CI 不同就会重红）；
  ⓒ 依赖另一层变量为空才生效——`t.TempDir()` 内部是 `os.MkdirTemp(os.Getenv("GOTMPDIR"), pattern)`，
  `GOTMPDIR` 为空时才回落到 `os.TempDir()`（读 `TMPDIR`）；**若 CI 或本地设了 `GOTMPDIR`，
  那种「用例内再 `t.Setenv("TMPDIR", …)` 改回去」的修法会静默失效**。
  **这类脆弱点的取巧验证法（与平台无关，无需目标平台的机器）**：直接对「覆盖机制本身」做两向对照，
  而不是去目标平台跑整套测试。把同一个最小用例跑两次、只改那个中间层变量，即可证明覆盖是否生效：
  `GOTMPDIR=""` → `t.TempDir()` 落在 `os.TempDir()` 下；`GOTMPDIR=<目录>` → 落在该目录下、
  用例内的 `TMPDIR` 被忽略。**「覆盖是否真的覆盖到了」比「修完之后是否绿」更值得验证**——
  前者能发现条件性失效，后者只反映当时那台机器。

### H. 格式与兼容性（语义判断，**本仓库最高危的改动面**）

> **与 G 的分工**：G 问「契约是否被测试钉住」，H 问「**已发布的数据格式是否仍然可读**」。
> 权威依据是目标仓库 `AGENTS.md` 第 1 节的兼容性条款与 `docs/ENCRYPTED-NOTEBOOK.md` 的策略，
> **不是实现现状**——实现现状本身就是被审计对象。

- **H1 格式变更缺少版本与兼容计划**：文档、资源、属性视图、密钥信封、备份、历史或同步格式被改动，
  却没有显式格式版本，或没有「旧格式仍可读 / 有可恢复迁移」的路径。
  修法方向是**保留兼容读或提供可恢复迁移**，不是要求用户删除或重建数据
- **H2 兼容性变更绕过认证或回退明文**：未知格式、损坏、认证失败必须**保留原始数据并返回错误**；
  以「让用户能打开」为由跳过认证或降级到明文，属于比原问题更严重的缺陷
- **H3 缺少上一版格式的回归夹具**：格式改动没有用**受支持的上一版格式**的夹具覆盖
  读、导出、历史、备份、恢复路径。这类缺口在单机上不可见，跨设备或恢复时才暴露
- **H4 派生数据的重建假设**：派生索引可以重建，**但前提是源密文先通过认证**。
  把「反正能重建」当成可以丢弃源数据的理由，是此类缺陷最常见的合理化

**克制条款**：`AGENTS.md` 明确「加密笔记本开发已完成，按已发布特性维护」。
因此 H 类各条的目标是**保持可读与可恢复**，不是推动格式演进；也不得重新生成 MasterSalt 或丢弃密钥来绕过不兼容。

### I. UI 状态与交互契约（语义判断，**本仓库第二大提交类别**）

> **为何单列**：`evidence.md` 实测 2499 条提交里 **UI/style polish 占 823 条（33%）**，
> 仅次于 bug fix 的 41%——它是本 skill 此前**零覆盖**的最大类别。
> 这些缺陷符合本 skill 的核心定义：「单侧看着都对，只有交叉或跨状态才暴露」，且用户能直接感觉到。
>
> **与「风格」的分界（不可越界）**：本判据只管**行为**——状态、可达性、约束、反馈；
> 不管**外观**——配色、间距、字体、动效曲线。后者属 style，按 `description` 的声明不在范围内，
> 需要的是设计审查与视觉对比工具，是另一套方法论。
>
> **a11y（无障碍）的切分同理**：键盘可达性、焦点管理、ARIA 语义、状态宣告属**行为**，
> 是本判据的一部分；而**对比度、配色区分度、字号可缩放**属**外观**，
> 交给 a11y 工具（axe / Lighthouse）与设计审查——**不要在本 skill 报告它们**。
> 这个切分是有意为之：「文字对比度不足」需要颜色计算与视觉基线，
> 而「焦点丢失」只能靠走一遍键盘流程，两者的取证手段完全不同。
>
> **权威源**：同一组件族的兄弟实现（同族互查）；对话框 / 菜单 / 停靠栏的既有交互约定；
> `app/appearance/langs/en.json` 的键集合与各语言的实际长度。
> **不要用审计者的个人偏好当预期依据**——「我觉得应该加确认框」不是缺陷。

- **I1 状态矩阵覆盖不全（UI 层的 D3）**：先把 UI 状态显式化为闭合集合——
  **空 / 加载 / 错误 / 只读 / 超长**——再逐组件比对「哪些状态有处理」。
  同一组件族的兄弟实现（多个列表视图、多个面板、多个对话框）中某一处漏了某个状态是最强信号。
  **不能只报「没有加载态」**：必须写出该状态下用户实际看到什么（显示上一次的数据？永久空白？可点击但无反应？）
- **I2 交互态残留（C 的镜像）**：C 问「该保的状态有没有保住」，本项问「**该清的状态有没有清掉**」。
  一次交互结束后应移除的临时态：`loading` / `disabled` / `active` / 拖拽占位 / 骨架屏。
  最典型形态是**异常路径提前返回**——正常路径有移除、抛错分支没有，
  于是按钮永久停在 loading、遮罩不再消失、拖拽占位不被清理
- **I3 焦点与键盘可达性（全新的推理形态：功能存在但用户够不到）**：
  ① 新增交互入口能否用键盘到达（`tabindex`、是否只在 `mousedown`/`click` 里处理、
  非原生元素上是否有键盘处理）；② 对话框 / 弹出菜单**关闭后焦点是否回到触发元素**
  （丢了则键盘用户要从文档开头重新走）；③ 焦点陷阱与 `Esc` 是否成对；
  ④ 纯图标按钮有没有可访问名；⑤ 焦点指示器是否被 `outline: none` 移除而未给替代。
  **这类缺陷在鼠标操作下完全不可见**，不会有测试报错，也不会有用户报 bug（只会觉得「这工具难用」）。

  **可机械检出的部分用 `scan_a11y_antipatterns.py`**（来源见 [全栈层面地图](./references/stack-map.md) 的「来源与许可」）：
  它只覆盖三类可靠形态——**K2** 正整数 `tabindex`（破坏 DOM 顺序，无任何正当场景）、
  **K5** `outline:none` 且同选择器无 `:focus` 替代、**A6** 纯图标按钮无可访问名；
  其余（`aria-hidden` 包裹可聚焦元素、`role` 缺必需 `aria-*`、焦点陷阱无 `Esc`、
  仅 `hover` 无 `focus`、关闭后焦点不回位）只给**提示位点**，必须人工判定。

  **两条上游硬约束（已内化）**：
  ① **不得把推测的辅助技术行为当作事实**——没有运行期证据就只能写成「需确认」；
  ② **「Lighthouse / axe 通过」不是无障碍的证明**，反之静态语义正确也不代表运行期没问题。

  **取证只能靠真实渲染 + 键盘**：用 `Tab` / `Shift+Tab` / `Enter` / `Space` / `Esc` 走一遍
  报告里的「业务表现」路径。`Tab` 顺序、焦点可见性、焦点回位**鼠标测不出来**。
  业务表现要写成键盘路径，例如「设置 - 外观，按 Tab 走到 X 按钮时看不到焦点位置」。
- **I4 受约束容器中的文本膨胀**：固定宽度 / `white-space: nowrap` / 单行截断的元素，
  配上非英文翻译会溢出或截断。用 `scan_i18n_text_expansion.py` 拿候选，
  **再回读该键的使用点确认容器是否真的受约束**——脚本只算长度比，它不知道谁在使用这个键。
  实测：`ja` / `zh-CN` 各仅 2 个候选，而 `de` 85 个 / `fr` 89 个（`Sync` → `Synchronisierung`、
  `Skip` → `Überspringen`）——**同一个布局问题只在部分语言下显形**
- **I5 破坏性操作的确认 / 撤销不一致（UI 层的 D1）**：同一组破坏性入口（删文档、删资源、清空历史…）中，
  某些有确认或可撤销、另一些没有。权威源是**同族中已有确认的那一处**，不是审计者的偏好。
  定性时不写「缺少确认框」，要写「哪一个入口与哪几个不一致」

**取证：UI 行为必须在真实渲染环境里验，不能只读代码。**

1. **读数不够**：状态残留与焦点问题常只在**特定操作顺序**下出现（先失败一次再成功、
   中途切走再回来），静态阅读看不到
2. 用**浏览器自动化 / Chrome DevTools 系列工具**走一遍报告里的「业务表现」路径；
   控制台报错与网络请求也一并看——I2 的异常路径常伴随一条被忽略的 console 错误
3. **焦点问题必须用键盘走**（`Tab` / `Shift+Tab` / `Esc`），用鼠标发现不了
4. 虚拟滚动、`/// #if MOBILE` 编译期剔除、跨端模板差异见 [全栈层面地图](./references/stack-map.md) L6 的取证陷阱

### 待实证的检查方向（**不是判据**，不占用字母、无 P 条目）

以下方向由通用运行时经验提出，**本仓库尚无确认的缺陷实例**。按模式库「从一次已确认的缺陷出发」的
扩充规范，取得实例后再升级为正式判据。命中时必须走完整挑战门——
缺少本仓库先例意味着它更容易是「可以更好」而非缺陷。

- **资源与生命周期**：长驻进程（内核）与长生命周期页面（编辑器）里，资源的创建点与释放点必须成对且可达。
  ① 订阅 / 定时器 / 句柄未释放——看**销毁路径**（`destroy` / 组件移除 / 页签关闭）是否覆盖全部创建点，
  **同族实现只有某一处漏了 `destroy`** 是最强信号；② 集合无上限增长——**与 D4 分工**：
  D4 问「裁剪端是否与消费端同端」（已有界但裁错端），此处问「**是否存在界**」；
  ③ goroutine 无退出信号（`goleak` 可检出）、锁持有跨越 I/O。
  层面定位与取证方法见 [全栈层面地图](./references/stack-map.md) 的「资源与生命周期检查点」。

**本仓库实证**：200 个 bug 修复提交中 **30% 同时补了测试**（详见 [实证数据](./references/evidence.md)），说明这些正是原有测试的盲区。**修复时补的测试类型，就是下一轮该主动检查的测试类型。**

## 执行流程

**先定模式**（默认审计）：审计 / 修复 / 架构调整 / 验证。后三种的授权边界与完整流程见
[修复与架构调整手册](./references/repair-playbook.md)。

1. **先读既有发现登记表，并把它当作去重过滤集**。来源至少两处：`references/evidence.md` 的历轮发现表，以及目标仓库的 memory 记录（如 `/memories/repo/`）。
   登记表里已有的「已确认 / 已提 issue / 已自行推翻」条目，其位置与结论即为过滤项。
   候选命中过滤项时，**本轮只允许追加证据**（新增实测、补充检查法、修正严重度），
   且必须在报告的「既往记录」字段里写明「第 N 轮已确认，本轮新增 X」，不得重新立论。
   若匹配范围有疑（同一文件的不同缺陷、同一缺陷的不同症状），先取该轮记录的 `file:line` 核对，再决定是否算重复。
   **这一步不能省**：机械扫描必然重复命中同一处，它是唯一能拦住重复报告的环节。
   **并行会话的额外四步**：① 编号分配必须「先提交先占用」并**立即 push**，只在本地写、最后才合并必然撞号；
   ② **建 issue 前**用 `gh api -X GET search/issues`（覆盖 open + closed）逐条检索，不能只查已确认的登记表——
   另一个会话的产出不在登记表里；③ 只读审查「同一处缺陷是否已由对侧报告」，命中则**不重复立论**，把新增证据作为评论追加；
   ④ **临时工作目录用唯一名**（`audit-r<N>-<会话后缀>`），清理时**只删自己建的文件，绝不递归删整个 `audit-r<N>`**——
   同一 skill 的两个会话容易撞同一套命名，递归删除会把对侧的中间产物一起删掉
   （第十八轮实测：我删了两次 `%TEMP%\audit-r18`，对侧文件随后被重建，说明当时仍在活动）。
   （第十六轮实测：两个会话同时跑同一轮，一处缺陷被双方独立命中，靠第 ② 步在提交前拦下。）
   **验证模式（用户要求「复核既有发现处理得怎样」时）**：跳过第 3–7 步，改走固定四步 ——
   `issue 状态（`gh api repos/<owner>/<repo>/issues/<n>`，注意显式 `-X GET`，`-f` 会把请求变成 POST）
   → 修复提交 → 代码现状 → 回归测试`。四条硬约束：
   ① **编号 → 提交不能只看提交信息**：修复常藏在**不引用该编号**的提交里（第十七轮实测：#19429 的修复在 `2b26d96ec6`，
   其提交信息只写 #19378），`git log --grep=<编号>` 空手而归时不能判定「未修复」，必须以代码现状收口；
   ② **修复思路可能与报告建议不同**：验证要落到**显示/消费链路的末端**，而不是比对实现是否与建议一致
   （第十七轮实测：#19435 维护者保留原始数值量纲、只覆盖 `formattedContent`，两条消费路径各取所需字段，修复成立）；
   ③ **先排除环境与既有失败**：本机平台差异（见「已知误报」）和与本次修复无关的既有失败会伪装成回归，
   判定前用 `git log -1 --format=%ad -- <file>` 核对改动时间是否落在修复窗口内；
   ④ **不要用输出尾部代替汇总行**：测试运行器的汇总（`fail N`）与逐条清单必须完整读取。第十七轮实测中我曾只读 `-Last 15`
   就写下「只有 1 处无关失败」，实际是 13 处（10 处为源码测试真实失败），**误判直接写进了结论**。
   同时先确认 CI 实际覆盖范围（判据 G4）——本地跑红的测试可能根本不在 CI 里，这本身就是要独立报告的发现。
   ⑤ **`fixed` 的结论必须看 CI 的实际结论，不能只看提交信息或维护者评论**：
   `gh run list --json databaseId,status,conclusion,headSha` → `gh run view <id> --json jobs`。
   实测一例：提交信息写「Run all kernel and frontend tests in CI」（看似已修复），
   但同一 commit 的 CI 为 `failure`；后续又需两个提交（`TMPDIR` → 单测级 `TMPDIR`）才转 `success`。
   同理，维护者评论里的「本地全量测试通过」也不等于 CI 通过。验证轮应以**最后一个引用该 issue 的提交的 CI 结论**收口。
2. **确认范围**。全仓库还是指定包？先问清楚，避免无边界扫描。
3. **跑机械筛选**。执行判据 A、F 的脚本，并按需扩展到其它层面：

   | 脚本 | 目标 | 判据 |
   |---|---|---|
   | `scan_duplicated_literals.py` | 充当标识符的重复字面量 | A |
   | `scan_unescaped_html.py` | 未转义的 HTML 插值 | F |
   | `scan_dom_type_literals.py` | 前端 `data-type` 闭合集合（权威源是 Lute 的 NodeType） | A、D3 |
   | `scan_doc_parity.py` | 多语言文档的结构与标识符一致性 | A、D3 |
   | `scan_i18n_text_expansion.py` | 受约束容器中的翻译文本膨胀（I4 的候选） | I4 |
   | `scan_a11y_antipatterns.py` | 可机械检出的 a11y 行为反模式（K2 / K5 / A6） | I3 |

   多语言文档的一致性**不属于任何别的校验器**：实测 `apigen` 只生成
   `app/src/types/api/index.d.ts`、`schema.json` 与 petal 的 `index.d.ts`，
   `docs/API*.md` 是**手写、各自维护**的，因此版本间漂移不会被任何生成步骤发现。
4. **并行语义判断**。对候选头部逐条判断；B/C/D/E 类用 subagent 并行核查，避免污染主上下文。
5. **过挑战门**。对每条发现按 [挑战门](./references/challenge-gate.md) 做两轮对抗审查。
   两个方向都要防（噪声报成缺陷 / 真缺陷判成噪声）；**降级理由是一条技术断言时必须再验该断言**；
   同时审「建议的修法」是否有害。
6. **强制取证**。每条发现给出文件:行与复现方式；能写测试的写测试。
7. **落回业务表现**。为每条发现写出「业务表现 / 预期表现 / 可验证不变量」三元组；预期表现的权威依据必须可引用。**触发条件不直观的，额外写出「普通操作看不到 + 什么输入才触发」的复现步骤。**
8. **输出报告**。按下方「输出格式」，用普通文本与小标题，不要用代码块包裹发现。
9. **写回判据库**。新判据进 [模式库](./references/patterns.md)，新误报进
   [已知误报](./references/known-false-positives.md)。

**进入修复模式后接续**（1–9 步已完成，且用户已明确授权修改）：

10. **先做变体分析**。同一缺陷不会是孤例，用判据 A / D 的机械手段扫同族；
    命中者纳入同一次修复，或明确列为后续项写进报告的「未尽事项」，不允许沉默略过
11. **评估爆炸半径再动手**。调用方、消费端字段、跨端影响、是否不可逆，四项都要写进报告；
    建议形如「照抄另一处实现」时，先确认两处的上游消费方是否相同
12. **按阶梯选修法，然后走验证闭环**。收口前必须确认该测试**在 CI 执行集内**（判据 G4），
    并以 CI 的**实际结论**收口——提交信息与维护者评论都不算。完整流程见修复手册第 3、5 节

## 输出格式

**不要用代码块包裹发现。** 代码块在对话里窄、不换行、无法折叠，长条目读起来很费劲。
改用下面的**一级小标题 + 字段列表**结构，每个字段一行，`字段: 值` 不加对齐空格：

### 主报告条目模板

> 用 `### 1. <一句话结论>` 作标题，标题里直接写清「什么东西 + 错在哪 + 用户会看到什么」。
> 下面逐行列字段，**字段名后用一个空格接冒号再一个空格**，不要用多空格对齐（对话里等宽字体不保证）。

---

### 1. `<一句话结论>`

- **判据**: A / B / C / D / E / F / G / H / I + 编号
- **既往记录**: 无（本轮首次发现）/ 第 N 轮已确认（issue #NNNN）—— 本轮新增 <实测证据 / 检查法 / 严重度修正>
- **位置**: `kernel/model/sync_path.go:153`, `kernel/util/runtime.go:345`
- **证据**: 可复现命令或行号引用
- **置信度**: 高（已实测复现）/ 中（代码可证，未实测）/ 低（需人工确认）
- **挑战门**: CONFIRMED / DOWNGRADED（原因）/ REJECTED / 未触发；**论据被纠正时要一并写明**（如 `CONFIRMED（论据已纠正：RFC 误引）`），否则后续读者会重复同一个推论错误
- **触发条件**: 什么输入/状态才会出现该行为；说明是否常见
- **业务表现**: 用户可见入口（用 `设置 - 快捷键` 这种路径写法）- 实际看到什么
- **预期表现**: 应该是什么；权威依据一句
- **可验证**: 一条可判定真假的等式/断言
- **影响**: 具体后果，不要写「可能有问题」
- **建议**: 最小修复方向，不做大重构提议

---

### 复现步骤（触发条件不直观时必写）

当缺陷需要特定输入才触发时，**必须单列一段复现步骤**，并明确区分：

- **普通操作下会看到什么**（通常是「正常」——否则用户早就报了）
- **需要什么特定输入才会出错**（给出可直接粘贴的名字/值）
- **为什么普通操作看不到**

缺失这段的描述，读者第一反应是「我试了，没问题」，报告会被直接丢掉。

---

置信度必须诚实标注。低置信度条目保留但明确标注，交由人工判断，不要凑数。

「业务表现 / 预期表现 / 可验证」三项为强制项。写不出业务表现且给不出预期表现权威依据的发现，不得进入报告主列表，只能作为附录观察项。

**修复报告**（修复模式下）用 [修复手册第 8 节](./references/repair-playbook.md) 的模板 R1：
字段结构与上方条目一致，另加「变体 / 爆炸半径 / 改法（阶梯第几级）/ 回滚 / 未尽事项」。

**对话中的输出约定**：

- 发现、结论、复现步骤一律用普通文本 + 小标题 + 列表输出，**不用围栏代码块**
- 代码片段用**行内代码**或最多 3-5 行的短引用；整段源码只给 `file:line` 让读者自己看
- 表格只用于「多条目横向对比」，单条目不要用表格（窄屏会折行拆散）

## 已知误报（去重过滤集）

过滤集已移至 **[已知误报](./references/known-false-positives.md)**（执行流程第 1 步去重时读）：
「不要报告的类别」22 条与「曾被误判为误报、实为真缺陷」的方法论提醒。

**为什么不在本文件**：该表持续增长、每条都很长，而它只在审计的去重环节需要；
放在本文件会让**每次触发**都加载它。本文件只保留热路径（决策、判据索引、流程、输出格式），
其余按 [参考资源](#参考资源) 的触发条件按需读取。

## 修复时的硬约束

排查结论**不是**动手改动的授权。**用户未明确要求修改时，本 skill 只做只读审计。**

修改前必须遵守目标仓库的 `AGENTS.md`：

- **禁止** `git commit` / `git push`，除非用户明确要求
- **只读审计禁用一切会改写工作区的命令**。已知的三个：
  - `pnpm run lint` 实际是 `pnpm run typecheck && eslint . --fix --cache`——
    `semi` / `quotes` 都是可自动修复规则，**会直接改写源码**，且 `--cache` 会写 `.eslintcache`。
    只读等价形式：`npx tsc -p tsconfig.typecheck.json`、`npx tsc -p tsconfig.api.json`、
    `npx eslint .`（**不要** `--fix`）。修复模式下才可跑带 `--fix` 的形式。
  - `pnpm test` 会重写 `app/pnpm-lock.yaml`
  - `pnpm build` **禁止**（会与开发者的 `pnpm dev` 冲突并产出坏包）
- 改 Go 代码后跑 `gofmt`，但**不要**编译内核二进制，也不要重启正在运行的内核
- 改 i18n 后跑 `python scripts/check-lang-keys.py`
- 涉及同步忽略规则、加密笔记本、快照格式时先读 `AGENTS.md` 兼容性条款——
  **忽略集合变动会导致其它设备签出时删除文件**

完整的修复流程（授权边界、爆炸半径、变体分析、修法阶梯、验证闭环、高危改动清单、提交与回读）
见 [修复与架构调整手册](./references/repair-playbook.md)。

## 维护本 skill

**追加与整理规范、本 skill 的版本管理（强制提交推送）、编辑本 skill 的坑，见
[维护规范](./references/contributing.md)**——这些只在**修改本 skill 时**需要读，审计与修复流程不需要。

> 注意区分两个仓库：**目标仓库**（`AGENTS.md` 禁止擅自提交）与 **本 skill 仓库**（每次改动后**必须**提交推送）。
> 两者规则相反，不要互相套用。

## 参考资源

**按触发条件读**，不是文件清单：

| 何时读 | 文件 | 内容 |
|---|---|---|
| 每轮开始（去重） | [已知误报](./references/known-false-positives.md) | 过滤集：不要报告的类别 + 曾被误判为误报但实为真缺陷 |
| 确定范围 / 定层 | [全栈层面地图](./references/stack-map.md) | L1–L10 的关键路径、权威源、高发形态→判据、仓库自带校验器、取证陷阱、跨层连带检查表 |
| 审一个 PR / commit / diff | [差分审查模式](./references/diff-review.md) | 按 diff 定范围、三类必查（漏改 / 新引入 / 声明不符）、本模式特有误报 |
| 过挑战门 | [挑战门](./references/challenge-gate.md) | 两轮对抗审查（审发现）+ 四问审查（审修法） |
| 需要某条判据的细节 | [模式库](./references/patterns.md) | P1–P38 的定义、跨领域实例、检查法、修法陷阱 |
| 修复 / 架构调整 / 验证 | [修复与架构调整手册](./references/repair-playbook.md) | 授权边界、爆炸半径、变体分析、修法阶梯、架构判据、验证闭环、高危改动清单、模板 R1 |
| 写「既往记录」字段 | [实证数据](./references/evidence.md) | 历轮发现登记表（去重的第二来源）与量化结论 |
| 修改本 skill | [维护规范](./references/contributing.md) · [更新记录](./references/changelog.md) | 追加与整理规范、版本管理、编辑坑；历次变更历史 |

`scripts/` — **六个机械扫描脚本与两个自检**：
`scan_duplicated_literals.py`（判据 A）、`scan_unescaped_html.py`（判据 F）、
`scan_dom_type_literals.py`（前端 DOM 契约闭合集合）、`scan_doc_parity.py`（多语言文档一致性）、
`scan_i18n_text_expansion.py`（判据 I4 文本膨胀）、`scan_a11y_antipatterns.py`（判据 I3 无障碍行为）、
`scan_regression_index.py`（历轮发现的回归索引，服务差分审查与修复验证）、
`test_scan_scripts.py`（脚本行为与过滤规则）、`skill_self_check.py`（本文档库的 7 组结构检查）。

## 更新记录

已移至 [更新记录](./references/changelog.md)（纯历史，审计流程不需要）。



