# 更新记录

> 各轮的完整实证、案例与教训见 [实证数据](./evidence.md)；本表只记「本轮改了什么」，不重复实证细节。
> **本文件是纯追加的历史**，与审计流程无关；修改本 skill 的规范见 [维护规范](./contributing.md)。

| 日期 | 变更 |
|---|---|
| 2026-09-13 | 初版。判据 A-G、挑战门、两个扫描脚本、实测数据 |
| 2026-09-13 | 移出仓库至用户级 `~/.agents/skills/`，脚本路径改为 `<skill-dir>` 形式 |
| 2026-09-13 | 第二轮：新增判据 D4（归约方向反转）、模式 P10；新误报「跨行已转义」 |
| 2026-09-13 | 新增核心原则「结论必须落回业务表现」；输出格式加「触发条件 / 业务表现 / 预期表现 / 可验证」强制项 |
| 2026-09-13 | 新增「本 Skill 的版本管理（强制）」：skill 仓库每次修改后必须提交并推送 |
| 2026-09-13 | 第三轮：D3 增补「跨语言同名常量表」判据与差异方向可相反的约束；新误报「跨语言同名常量表的差异」；新增模式 P11 跨实现常量表漂移 |
| 2026-09-13 | 第四轮：新增模式 P12 同义字符串变换的多实现漂移（未锚定首次匹配）；新误报「同义解析函数的边界差异」 |
| 2026-09-13 | 输出格式改为「小标题 + 字段列表」，明确禁止用代码块包裹发现；新增「复现步骤（普通操作看不到 / 什么输入才触发）」为触发条件不直观时的必写项 |
| 2026-09-13 | 第五轮：D1 增补「同构分支逐项 diff 判定条件 + 恒真守卫」检查法 |
| 2026-09-13 | 第六轮：D1 增补「恒假守卫」检查法（守卫预设的实参形态与全部调用点不符，函数体沦为死代码）；新增模式 P13 |
| 2026-09-13 | 第七轮：D3 增补「能力白名单」判据（权威源为上游类型方法，同包委托上游的等价函数自相矛盾即强证据；按特性时间线区分漏项与有意排除；修法不等于补齐全部上游成员）；新增模式 P14 |
| 2026-09-13 | 第八轮补记：D3c 增补「同一契约内 struct 与 map 并存时优先怀疑 struct 型成员」；补入「类型过滤真实门控结果（301→311）」的端到端取证与「写入→回读→删除」零残留复现法；证据与生成物/文档同源，不构成独立权威侧 |
| 2026-09-13 | 第八轮：D3 增补 D3c「跨契约边界的手写 DTO」（序列化边界的漏项由一致性缺陷升级为静默数据丢失；指针强转要求修复在等价位插入；不变量取「写后读键集合守恒」）；新增模式 P15 |
| 2026-09-13 | 新增执行流程第 1 步「先读既有发现登记表并去重」，核心原则 6 增补「去重与累积同等重要」，报告模板新增强制字段「既往记录」 |
| 2026-09-13 | 第十一轮：新增判据 D1 的「同一算子在多条实现路径上的量纲/格式漂移」检查点（grep 枚举常量名 + 其字符串字面量，逐路径 diff 量纲/数值类型/格式标记；整数除法反例优先）；新增模式 P18；新误报「跨语言重复的枚举字符串」 |
| 2026-09-13 | 第十一轮补记：补「修复影响面」取证方法（量纲与格式必须同改，只补格式会得 `3300%`；模板字段读原始 `content` 故补 `%` 无效；筛选/底部汇总/排序/迁移逐项定性）；回填历轮「待提 issue」为已提编号，使登记表可直接当去重过滤集；已提 issue #19435 |
| 2026-09-13 | 第十二轮：D1 增补 D1d「同族方法只有一处缺前置守卫 + recover 中间件把 panic 静默转成 2xx」（验证链：同族 not-found 分支计数 → 调用方按路径形态而非存在性分派 → panic 的最终 HTTP 表现；严重度看 panic 之前的副作用；修法不等于照抄兄弟路径）；新误报「审计者自行推断的上游状态码/协议映射」；新增模式 P19 |
| 2026-09-13 | 第十三轮（前端定向）：D1 增补 D1e「同族多实现里唯一一处读错 DOM 属性」（恒空需证明无写入方；上游按 DOM 属性分派且该属性每次渲染被规范化时侥幸路径被关死）；新增模式 P20 同族读错属性、P21 手写配置登记表漂移、P22 同源请求构造的复制品漏字段；新误报「跨端设置项不生效的差异」 |
| 2026-09-13 | 第十三轮补记：四条第 1/2/3/4 项已提 issue #19440（看板搜索读错属性）、#19441（catalog 与菜单声明漂移）、#19442（移动端替换模式漏 searchHPath）、#19443（移动端只读空页显示新建文档）；登记表已回填编号，可直接当去重过滤集 |
| 2026-09-13 | 第十四轮：D4 增补「有界集合的容量裁剪必须与消费端同一端」检查点；D1 增补 D1f「同一行重复同一表达式（漏改下标，含 `splice(indexOf())` 的 -1 陷阱）」与 D1g「同类宿主页面的配置不对称（Electron webPreferences 实例）」；新增模式 P23 裁剪端反转、P24 重复表达式、P25 宿主配置不对称、P26 临时展现态无载体；新误报「声明了但永不填充的模型集合」 |
| 2026-09-13 | 第十四轮补记：五项已提 issue #19446（远端启动页缺 nodeIntegration 致连接按钮失效）、#19447（最近关闭栈裁剪端反转）、#19448（定位折叠分组重渲即丢失）、#19449（浮动停靠栏 resize 重复第一组漏第二组）、#19450（交换对比方向清空对比区）；登记表已回填编号 |
| 2026-09-13 | 第十五轮：D3c 增补「前端键集合 ⊆ 内核 struct 键集合」检查点（权威侧可反转；缺字段使对象深比较守门恒不成立 → 附带无谓全命名空间写）；D1 增补 D1h「词法路径校验在符号链接面前失效（叶子 vs 中间组件）」与 D1i「异步队列吞掉落盘错误使调用方假成功」；新增模式 P27/P28/P29；新误报「『不跟随符号链接』的文件系统断言只对叶子成立」 |
| 2026-09-13 | 第十五轮补记：四项已提 issue #19452（通知开关 selectAllIncompleteTip 永无效）、#19453（模板删除词法校验可被中间组件软链越界）、#19454（设置面板 textarea 未转义）、#19455（MCP inbox 缺加密租约 + 异步事务吞掉落盘错误）；`tag`/`bookmark` 与插件能力注销键两条经挑战门驳回；登记表已回填编号 |
| 2026-09-13 | 第十六轮（B 线：内核 CLI / server / sql）：D1h 增补「免鉴权前缀 × 纯词法校验 × 跟随软链」组合与两处修法陷阱（不可复用 conf 前缀黑名单、读分支同需加固）；新增 D1m「CLI/脚本入口的假成功（异步入队 / 无返回值 / WS-only 错误通道 / dry-run 被参数条件吞掉 / 空结果截断文件）」；已知误报新增「断言只读角色可调用漏挂 CheckReadonly 的端点」；新增模式 P34 免鉴权静态出口的软链越界、P35 CLI 假成功、P36 库默认白名单被选项覆盖、P37 谓词通配符未转义 |
| 2026-09-13 | 第十六轮（未覆盖区域定向）：D1d 增补「同族守卫挂载是保守惯例，读端点也挂，须先按有无副作用分类」；新增 D1j「同族路径上前置守卫的位置漂移（副作用之前 vs 之后）」、D1k「模板条件隐藏集合 ⊋ 运行时恢复集合（含 `previousElementSibling` 跨端语义）」、D1l「同族复制粘贴读错绑定对象（对对象读 `.length`）」；新增模式 P30/P31/P32 与两条已知误报 |
| 2026-09-13 | 第十六轮补记：四条已提 issue #19462–#19465；第 1 条与并行 B 线会话的 #19461 重复，未重复提交，改为补评论（机制层证据）。**新增并行会话规则：编号「先提交先占用」并及时 push，提交前用 search API 覆盖 open+closed 去重** |
| 2026-09-13 | 第十七轮（修复验证）：新增 **G3**「测试替身白名单与源码 import 图无一致性断言」；执行流程新增「验证模式」四步法与硬约束（翌日补正至五条）。**本行为历史结论：「宿主平台 MIME 注册表差异」当时被列入已知误报，已在下一行改判为真缺陷** |
| 2026-09-14 | 第十七轮补正：G3 拆分三种失败形态（缺模块 / 桩过时 / DOM 替身缺方法）并补两个影响倍率；新增 **G4**「测试不在 CI 执行集内」；「验证模式」加第四条硬约束「不要用输出尾部代替汇总行」并要求先确认 CI 覆盖范围；把「MIME 注册表」从误报改为**真缺陷**（内联白名单来源是宿主注册表，已提 #19475）；已提 #19472–#19475 |
| 2026-09-14 | 并行会话规则由三步扩为四步：新增「临时工作目录用唯一名，清理时只删自己建的文件，绝不递归删整个 `audit-r<N>`」（本轮实测撞名并误删对侧产物） |
| 2026-09-14 | 第十八轮（API 契约重构遗留缺陷）：新增 **D1n**「适配层新增的规范化标记改变了安全敏感的既有输入」与模式 P38；已知误报新增「请求字段的 `trim` 看似只是宽松化」；已提 issue #19477（主密码含首尾空格无法解锁加密笔记本） |
| 2026-09-14 | 修复验证收官：G4 增补「补全覆盖后以三连提交形式暴雷」的环境前提检查点（夹具临时目录 × 硬编码路径黑名单的前缀关系）；验证模式增补第五条硬约束「`fixed` 必须看 CI 的实际结论，不能只看提交信息或维护者评论」（实测一例：提交信息声称已全量跑测试，该 commit 的 CI 实为 failure，又经两次修复才转 success） |
| 2026-09-14 | G4 增补「矛盾前提的 2×2 取证法」与「依赖环境默认值才成立的修复」三个脆弱点（平台守卫 / CI 取值 / `GOTMPDIR` 架空用例级覆盖），并给出**取巧验证法**：对「覆盖机制本身」做两向对照，而不是去目标平台跑整套测试；已提 issue #19480 |
| 2026-09-14 | **历史记录整理**（三档）：① 修正 `已知误报` 表格被空行打断的格式问题，并把已改判为真缺陷的 MIME 条目移入新子节「曾被误判为误报、实为真缺陷」；修正更新记录中已被推翻但并列表述的旧结论；② `evidence.md` 把第十二轮/第十八轮从 `###` 提升为 `##` 并移到时间序正确的位置、「如何更新本文」移到最后、合并第十七轮重复的两张 issue 表、去掉更新记录每行重复的「实证见 evidence.md」；③ 把 `D1b`–`D1n` 的完整检查法与实例收到模式库，`SKILL.md` 每条只留「判据 + 关键信号 + P 编号」（D 节 100 → 41 行） |
| 2026-09-14 | **自审与修复**（对 skill 自身套用本判据）：① 两个扫描脚本在扫描根不存在时输出「0 个文件 / 0 条发现」并以 0 退出——正是判据 D1m / P35「假成功」，会让「没扫到」被当成「没问题」，改为报错 + 退出码 2；② `--min-files` 默认是 2，而 SKILL.md 明确警告「降到 2 会引入大量偶然命中」且脚本 docstring 也示范 2，三处不一致，默认改为 4（实测 2 → 1060 条、4 → 274 条）；③ 新增 `scripts/test_scan_scripts.py` 自检（防上述两项回归 + 目标仓库冒烟）；④ E2 由一行扩为可操作检查法并补三类实测（注册表覆盖 MIME 内置表 / CRLF 换行符 / 临时目录是否命中硬编码路径黑名单），补「不要用『我这里正常』排除」的反驳写法；⑤ 挑战门补四条实测教训：两个方向的误判（真缺陷被判成噪声更贵）、降级理由是一条技术断言时必须再验（第一轮也会错）、裁决之外要记「论据被纠正」、审建议的修法是否有害；⑥ 报告模板「挑战门」字段要求写明论据纠正 |
| 2026-09-14 | **校准判据 F 并压缩 D3/D3c/D4**：① 实测 F 脚本的误报率——785 文件 / 244 候选 / 95 文件，归一化后只有 **43 种形态**，抽样 100+ 条判为真缺陷 **0 条**；「条数」不等于工作量，应按形态逐类判定。列出三类真正需人工判断的（裸变量 / DOM 与接口驱动的值 / 跨模块返回值）与六类可直接跳的（已在 SKILL.md 判据 F）。② 按实测扩充过滤规则（`Constants.*`、裸大写下划线常量、`escapeAriaLabel`、`updateHotkeyTip`、双字面量三元、数值字段尾随形态），候选 244 → **168（-31%）**；规则改为规则表并显式声明锚定方式（第一版把多条规则写成一个 alternation，其中一条备选的 `\s*` 能匹配空串，会让**全部**候选被判为安全）；③ 自检加 15 个应放行 + 8 个应保留的形态锁定，后者更重要；④ `D3`/`D3c`/`D4` 的细节已确认在 P14/P15/P27/P10/P23 中，压缩为「判据 + 关键信号 + P 编号」（D 节 62 行）；⑤ 新增「追加与整理规范」与 `scripts/skill_self_check.py`（表格连续性/列数、标题重复、轮次嵌套与递增、引用路径），负向验证 **6/6** 注入问题均被拦住；它在首次运行就抓到一个真缺陷——`已知误报` 表的原因列含未转义的 `this.mobile \|\| …`，该行在 Markdown 里被拆成 4 列 |
| 2026-09-14 | **扩展为全栈「查找 + 修复」skill**：① 新增 [全栈层面地图](./stack-map.md)，按 10 个层面（内核 L1–L5 / 前端 L6–L8 / 横切 L9–L10）给出关键路径、权威源、高发形态→判据、**仓库自带校验器**与取证陷阱，并加「跨层连带检查表」；把「优先跑仓库自带校验器、不在 skill 里重写第二真源」写成设计约束（这正是判据 A）；② 新增 [修复与架构调整手册](./repair-playbook.md)：授权边界、爆炸半径四问、变体分析三扫法、修法阶梯五级、架构调整五条判据与最低要求、验证闭环六级、高危改动清单、提交与回读、报告模板 R1；③ 新增判据 **H 资源与生命周期**（标注「尚无本仓库确认实例」，故不建 P 条目）与 **I 格式与兼容性**（对准 `AGENTS.md` 加密笔记本硬约束，并与 G 明确分工）；④ 新增「工作模式」表（审计 / 修复 / 架构调整 / 验证）与「全栈层面导航」，执行流程补修复模式接续第 10–12 步；⑤ `description` 与 `argument-hint` 扩展到修复与架构触发词；⑥ 追加与整理规范补第 9 条（`references/` 文件双向挂接、判据字母与 P 编号不得重复）；⑦ 层面地图与手册的方法论来源在文末标注，并声明 `trailofbits/skills`（CC-BY-SA-4.0）**只可参考、不得复制正文进任一仓库** |
| 2026-09-14 | **按「AI 加载成本」重组文件结构**：① 把三个**冷区**移出 `SKILL.md`，改为带触发条件的指针——[已知误报](./known-false-positives.md)（每轮读，但只在审计时）、[更新记录](./changelog.md)（纯历史）、[维护规范](./contributing.md)（只在改本 skill 时）；`SKILL.md` 由 618 行降至约 500 行，只留热路径（决策、判据索引、流程、输出格式）；② `description` 由 1240 字符压到约 880，保留触发词、去掉重复枚举；③ 判据 **H 资源与生命周期**（尚无实例）**降级为层面地图里的检查点**，不再占用判据字母——判据清单恢复「全部有实证」的完整性；原 **I 格式与兼容性** 顺位改名为 **H**，字母 A–H 连续；④ 「参考资源」由文件名列表改为**「做 Y 时读 X」的触发条件表**，让按需加载有明确判据；⑤ 自检扩至 **7 组**，新增「`references/` 双向挂接防孤儿」「判据字母唯一」「P 编号唯一且递增」，负向验证 **7/7**；⑥ 维护规范补第 10 条「热点区保持精简」与第 11 条「编号只增不重排（P33 空缺是历史避让，不要补齐）」 |
| 2026-09-14 | **新增差分审查模式与文档一致性扫描**：① 新增 [差分审查模式](./diff-review.md)——按 diff 定范围（而非目录），单列它的理由：`evidence.md` 实测 **58.5% 的 bug 提交只碰 1–2 个文件、中位数 31 行**，小 diff 才是主流场景，而它有一个审计模式看不到的产出：**「漏改」**（同一次改动里对称的一侧没改）。内容含三类必查、按改动性质分类而非按文件分类、本模式特有误报。② 新增 `scan_doc_parity.py`：比对多语言文档的**结构指纹**与**语言无关标识符**（反引号内、含 CJK 与 JSON 示例的剔除）。实测 `docs/` 下 10 组文档，**5 组有真差异**；其中 `API.ja.md` 结构与标识符双差异（缺 `TypeScript contracts` 一节）。**关键前提经核实**：`apigen` 只生成 `app/src/types/api/index.d.ts`、`schema.json` 与 petal 的 `index.d.ts`，`docs/API*.md` 是**手写各自维护**的，因此版本间漂移不会被任何生成步骤发现。③ 两类假阳性经实测后过滤：「标识符」不该含 CJK 或 JSON 示例（不加时 `API.ja.md` 报 12 条假差异）、**交叉引用本地化是正确行为**（中文版指向 `X.zh-CN.md`；不加抵消规则时 SY-FORMAT/TAB-BLOCK/WORKSPACE 三组各报 1 条，抵消后 TAB-BLOCK 转为一致） |
| 2026-09-14 | **第十九轮：多语言文档漂移（新判据 D3d + 模式 P39，已提 #19482）**：① 新增 `scripts/scan_doc_parity.py`，比对多语言文档的**结构指纹**（各级标题数/围栏数/表格行数）与**语言无关标识符**（反引号内、归一化后）；实测 `docs/` 下 10 组文档中 **4 组有差异**，其中 **3 处为真缺口**（「渲染模板」节缺 `mode`/`docTreePlan` 影响 zh-CN 与 ja；ja 缺整个 `TypeScript contracts` 节；`API-CONTRACTS.zh-CN.md` 缺 `TMPDIR` 与前端测试发现范围两段），均已提 issue #19482。② **关键前提经核实**：`apigen` 只写 `index.d.ts`/`schema.json`/petal，`docs/API*.md` 是手写各自维护的，漂移不会被任何生成步骤发现。③ **四类假阳性全部实测后修掉**：占位符名被译（`<relative-path>` 与 `<相对路径>`）、示例实参名被译（`filepath.Base(path)` 与 `filepath.Base(路径)`）、自然语言示例路径、交叉引用本地化。**核心修法是「归一化」而非「过滤」**——第一版用「含 CJK 则剔除」把中文版整条丢弃、英文版留下，反而制造「有而本版无」。④ 两个「修法自身出错」的陷阱：示例路径规则必须在归一化**之后**判定（否则只丢英文形式、规则自身不对称）、分桶必须按归一化后的值（否则占位符内含空格的等价值落入不同桶）。⑤ 判据 D3d 挂接 P39；实证数据新增第十九轮；本轮同时确认「结构指纹」与「标识符差集」盲区互补，缺一会漏掉其中一半。 |
| 2026-09-14 | **第二十轮：UI/UX 层面整合（新判据 I + 模式 P40 + 层面 L11）**：① 起因是量化发现的空白——`evidence.md` 实测 2499 条提交里 **UI/style polish 占 823 条（33%）**，仅次于 bug fix 的 41%，却是本 skill **零覆盖**的最大类别。② 新增判据 **I「UI 状态与交互契约」**（I1 状态矩阵漏项 / I2 交互态残留〔C 的镜像〕 / I3 焦点与键盘可达性〔全新推理形态：功能存在但够不到〕 / I4 受约束容器文本膨胀 / I5 破坏性操作确认不一致），并**明确划出与「风格」的边界**——只管行为（状态、可达性、约束、反馈），不管外观（配色、间距、动效），后者按 `description` 的声明不在范围内。③ 新增层面 **L11 交互与状态**（单列理由：交互横跨 L6 模板、L9 语言文件与宿主键盘/焦点，按目录归属哪一层都不完整；且它是唯一没有既有校验器可依赖的层面）。④ 新增 `scripts/scan_i18n_text_expansion.py`：按「英文 ≤12 字符且膨胀 ≥2x」取候选，再用 `--source` 交叉核对使用点是否受约束（`<option>` / `nowrap` / `ellipsis` / 同行固定宽度）。实测 SiYuan：长度候选 **488 个**（CJK 语言仅 1–4 个，de/es/fr/ru 各 80+、ar 76），经源码核对收敛到 **70 个**（86% 是噪声）。⑤ 新增模式 **P40**：含四形态定义、历史面板 `<select>` 的 7 个 `<option>` 实测案例（`en="sync"` → `de="synchronisieren (sync)"`）、以及「修法优先容纳而非改译文」的优先级。⑥ 修复手册第 5 级（端到端）补齐**具体做法**——前四级跑命令完全覆盖不到 UI 行为，必须用真实渲染环境、按报告的触发条件、键盘走一遍、**并切到译文最长的语言再走一遍**。⑦ 自检扩至 **8 组**，负向验证 **5/5**（含一条对真实仓库的验证：退化为不过滤后 70 → 409）。 |
| 2026-09-14 | **第二十二轮：设计令牌契约（`scan_css_token_contract.py`）+「组件化」的量化证伪 + 修掉一个自己的真缺陷**：① 起因是用户要求整合「组件化 / 设计组件 / 设计交互」类 UI/UX skill；先量化再动手，结论是**「组件化」在本仓库不构成可检缺陷类**——重复 HTML 片段 264 种（≥4 次）中靠前的全是自带工具类（`fn__space` 526、`fn__hr` 179、`fn__flex` 116，均已在误报表内）；组件调用点属性差异几乎全是调用点自有的 `data-*` 标识（`.b3-switch` 表面 25 种属性集、`.b3-button` 87 种，而契约属性其实执行得很齐：`type` 114/114、`aria-label` 159/159；`.b3-select`/`.b3-menu__item` 等根本没有契约属性，契约就是那个类）。**因此不建扫描器、不新增判据，只把量化结论与「覆盖率分离法」写进误报表与层面地图**。② 新增 `scripts/scan_css_token_contract.py`：设计令牌（CSS 自定义属性）契约的**三条件合取**——无 fallback ∧ 所有样式根（含主题）无定义 ∧ 源码从未提及。实测 192 个无 fallback 引用 → 条件②排除 167、条件③排除 20（写入）+3（仅提及）、另 5 个有 fallback 引用 → 候选 **2 条**；**只做前两个条件时 34 条候选里 17/17 全是假阳性**（都由 `setProperty`/`removeProperty`/注入的 CSS 模板串写入）——即 **CSS 自定义属性是本仓库 JS→CSS 的运行时通道**，只查样式表必然产出噪声。③ **条件③的宽规则是被一次漏检逼出来的**：窄的形态匹配（`setProperty("--x")` 等）把 `--b3-font-family-editor` 报成候选，而它与 `--b3-font-size-editor` 写在同一个注入的 CSS 模板串里（`util/assets.ts:404` 起），带 backtick 锚点的规则抓不到同串第二个变量名；改为「源码里任何位置提及过即算」的宽规则后修复。**形态匹配只用于输出说明，排除判定一律用宽规则**。④ 两条残留候选回读后**均为死规则**：`_pdf.scss` 是从 Mozilla PDF.js 移植的（Apache-2.0 头），`--loading-icon`/`--main-color` 在 PDF.js `viewer.css` 的 `:root` 里定义、移植时未带上，而唯一使用点 `.toolbarField.pageNumber.visiblePageIsLoading` 的类名 `visiblePageIsLoading` 全仓 **0 处**、`#errorWrapper` 无任何 JS 取消 `hidden='true'` → 按既有标准属「无消费方即无业务表现」的观察项。⑤ **发现并修复一个自己的真缺陷（跨盘崩溃）**：`scan_a11y_antipatterns.py`（5 处）与 `scan_i18n_text_expansion.py`（1 处）的 `os.path.relpath(p)` 未传 `start`，以 **cwd** 为基准；扫描根与 cwd 不同盘时（`d:\...` vs `c:\...`）抛 `ValueError: path is on mount`，**整个脚本崩溃、一条结论都输不出**。该缺陷长期存在而未被发现，因为自检恰好在与仓库同盘的 cwd 下跑过——**测试结果依赖 cwd 的通过是假通过**；统一改为 `rel_path(path, roots)`，并在自检里加**异盘 cwd** 用例。⑥ 自检扩至 **11 组**（第 10 组三条件降噪、第 11 组异盘 cwd），负向验证 **3/3**；**且其中两条断言在负向验证中被发现不承重并改掉**：原第 11 组只测「不崩」，而带 `try/except` 的退化会「吞掉异常 + 返回绝对路径」→ 补测「输出不含绝对路径」；原第 10 组断言的是「条件③排除 > 0」，那实际测的是**窄**规则（`writes`），把宽规则关掉后照样通过 → 改断言宽规则本身，并把阈值从「< 无 fallback 总数」收紧为「< 1/10」。⑦ 误报表 **+3 条**（CSS 令牌的运行时通道、组件调用点属性差异、移植样式里引用方已被裁剪的令牌）；判据 I 增「CSS 侧取证的两条硬前提」（JS 通道 + SCSS 嵌套 `&:伪类`：全仓嵌套 `&:hover` 154 处、`&:focus` 32 处、`:active` 16 处、`:disabled` 7 处，只 grep 选择器文本会把 `.b3-button` 判成「缺 hover/active/focus」，而按大括号配对后它实际拥有这四个）；层面地图 L11 补 5 条取证陷阱与设计令牌契约检查法；「来源与许可」新增**「部分采纳」小节**（`penpot-uiux-design` 的 Design Tokens 概念被采纳、其 Penpot MCP 与全部视觉条目不采纳）与拒绝清单（`oo-component-documentation`、`salesforce-component-standards`、`fluentui-blazor`、`mvvm-toolkit*`、`react19-*`、`react-container-presentation-component`——**载体在目标仓库不存在**，本仓库无组件目录、无组件注册表。唯一可迁移的「渲染归 ui / 有副作用归 features」原理已作为判据 D 的检查点记录）；维护规范补第 **14** 条（脚本输出路径必须以扫描根为基准）与两条「编辑本 skill 容易踩的坑」（负向验证的补丁要按**函数体**打，整串替换会误伤 `def rel_path(path, roots):` 定义行导致 import 期 `SyntaxError`；补丁必须真的**复现原缺陷形态**，否则用例不承重） |
| 2026-09-14 | **第二十三轮：按「零产出不进 skill」移除扫描脚本**：① 用户定调「零产出的脚本不该进 skill」，移除 `scripts/scan_css_token_contract.py`（第二十二轮加入，在目标仓库降至 2 条候选、回读后 0 条真缺陷）。**移除的是脚本，不是知识**——三条件合取规则、条件 ③ 的宽规则教训、两条死规则的案例全部保留，只是从「可执行」改写为「可阅读」：**判据 I** 的「CSS 侧取证的两条硬前提」（含 `--b3-font-family-editor` 漏检案例）、**误报表三条条目**、**层面地图 L11** 的既有校验器与取证陷阱（含五种 JS 写入形态清单与全部实测数据）、**evidence 第二十二轮**的完整实证。② 在 `SKILL.md` 的脚本清单里新增**准入标准**：脚本的价值按**在目标仓库的产出**衡量，不是按它的规则有多启发性；一个规则若只换来「一两条还要人工回读的候选」，它就是文档而不是脚本——脚本位与维护、自检成本是实成本。**「值得知道」与「值得自动化」是两件事**。③ `test_scan_scripts.py` 的自检由 **11 组变为 10 组**（删原第 10 组三条件降噪，异盘 cwd 组由第 11 组顺位为第 10 组），全通过；负向验证 3/3 的结论已固化进 evidence，不再随脚本存在。④ 顺带发现并记录三处小问题：自检组号**从 [3] 直接跳到 [5]**（原 [4] 组已并入 [3] 但无记录，跳号会让人以为漏跑一组）——按既有「编号只增不重排」约定（同 patterns 的 P33 空缺）**不重排**，改为在 docstring 里显式登记该空档；docstring 里的 `d:\...` 触发 `SyntaxWarning: invalid escape sequence`，改为不带反斜杠的写法；**本条目第一次追加时锚点选在了上一条的行中间，把第二十二轮的剩余内容切成了孤立文本**——已恢复重做，并把该形态补进维护规范第 4 条 |
| 2026-09-14 | **第二十四轮：补上「组件化 / 设计系统 / 交互设计」半区，并把上轮丢弃的维度做完**：① 用户质疑「整合 UI UX skill 是不是一直都没做」——**量化后确认两处失败**：把「组件化」用弱探针（只查重复 HTML 字符串）过度推广成整个维度的否定结论，以及**测「组件状态覆盖」时方法错了、发现后只记录了陷阱却没有改正重测**（用选择器文本判状态，而 SCSS 的状态主要写成嵌套 `&:伪类`，选择器文本不含组件类名）；覆盖数据也印证：`组件设计/设计系统` 在 `SKILL.md` 里 **0 命中**、`交互设计` 仅 2 命中。② **先把闭环补上**（上一轮工具已产出候选但一条都没落地）：逐条回读 K5 的 18 条「确认」与 A6 的 37 条，在运行中的实例（3.8.4-alpha.9）用真实渲染环境验证后提了 **#19493**（开关无焦点指示，含对照组实验：`.b3-button`/`.b3-text-field`/`.b3-select` 聚焦后各有 1 项变化、`.b3-switch` **0 项**）与 **#19494**（图标按钮无可访问名，37 处，四处均为「同一模板里部分有 aria-label、其余没有」的同族不一致），并在 #19493 补一条评论给出**系统性根因**：`util/_reset.scss:287-296` 对 `button, input, select, textarea` 一律 `outline: none`，全仓无全局兜底 → 1103 个表单控件使用点中 **462 个无焦点指示**（39 条 vendored PDF.js，自研 423 条）。③ **新增 `scripts/scan_focus_coverage.py`**（判据 I3 ⑤）：**它的核心价值是四个判定前提，四个都是我实测写错过才对的**——SCSS 嵌套的**后代语义**（只替换 `&` 会把 `.protyle-preview__action button:focus` 读成全局兜底，真缺口被抹掉）、**`transform` 也算可见变化**（漏掉它会把 `.b3-slider` 误报成缺口）、**按完整类集合而非单类判定**（焦点样式可能来自兄弟类）、**兜底按真实标签匹配**（`button:focus` 不覆盖 `<input>`）；为让自检能测真函数而非副本，判定逻辑从 `main()` 提到模块级。④ **修掉 A6 的一个系统性漏检**：`VISIBLE_TEXT` 会把 JS 字符串末尾的 `';` 当成按钮内文字，使**单行收尾的图标按钮被静默跳过**（实测 SiYuan 的 A6 由 37 条降到 31 条，漏掉的 6 条里 4 条集中在 `protyle/toolbar/index.ts` 的同一个模板）。⑤ 误报表 **+4 条**（`.b3-slider` 的指示是 transform、contenteditable 内容区不需要焦点环、K5 的「已确认」里多数是容器、vendored PDF.js 不按自研标准要求）；层面地图 L11 补 6 条取证陷阱；判据 I3 ⑤ 写入「本仓库的机制」与「无焦点指示不等于缺陷」的四项排除；维护规范补第 15 条与两条编辑坑。⑥ 自检 **10 组**（新增第 11 组「四个判定前提都不得退化」+ A6 裁切断言），**负向验证 5/5**，其中一条在首次运行时**不承重**——夹具让被覆盖的类恰好排在字母序最前，「按类集合」与「只按第一个类」结果相同，换成能区分的夹具后才承重 |
| 2026-09-14 | **第二十五轮：定位参照帧检查点（无确认实例，诚实地不占判据字母）**：① 起因是上轮结尾列的第二个候选方向——`SKILL.md` 里 `z-index` 与「定位上下文」命中数为 **0**，而 `AGENTS.md` 第 7 条明文要求「改 `position`/`transform`/`contain`/`overflow` 时检查对后代定位参照帧、overlay 覆盖、裁剪的影响」，属**现成权威依据**。② **属性表是实测的**（本地 HTML + `offsetParent !== null`，19 个用例），纠正三个常见误解：会让后代 fixed 改参照帧的是 `transform`/`filter`/`backdrop-filter`/`perspective`/`contain: layout\|paint\|strict`/`will-change: transform\|filter`；而 **`isolation`/`opacity`/`position + z-index` 不会**——它们只创建 stacking context，**不创建包含块**，把两者等同会把三个最常用的布局属性全部误判为危险。③ 业务表现实测：同一段 CSS 的 fixed 元素，无该祖先时 `top` 为 **0**，有 `transform: translateX(1px)` 时为 **400**（容器位置）——**偏 400px**，且这种「几乎等于没有」的 transform 正是它隐蔽的原因（改动者看不出任何视觉变化，坏的是后代）。④ 判定方法零假阳性但**必须做对照组**：首次在运行实例上跑得到 `4 fixed / 0 trapped`，靠同一次执行里的「有/无 transform 祖先」一正一反对照才敢认定页面确实干净——**没有对照的「0 个」分不清「干净」与「方法失效」**。⑤ 静态规模：`transform` 113 / `filter` 21（含 `backdrop-filter` 12）/ `will-change` 8 / `contain` 6、`position: fixed` 51 + 5 内联、`z-index` 24 种取值（动态分配基数 **10**）。⑥ 点名一处**隐式假设**（判据 E 形态）：`.mobile-topbar`/`.mobile-bottom-bar`/`.side-panel`/`.b3-menu--sheet`/`#editor > .protyle-breadcrumb` 都是带 `will-change: transform` 的移动端容器——当前后代无 fixed，但这些属性使其成为**禁地**；权威侧是 `.av__mask` 挂到 body（`cell.ts:648`）。⑦ 落地位置：层面地图新增「定位参照帧检查点（待实证）」节（与「资源与生命周期检查点」同一处理，**不占判据字母、不建 P 条目**）、修复手册第 5 级新增第 7 步（改这些属性后的必查项）、`SKILL.md` 判据 I 的「CSS 侧取证」由两条硬前提扩为**三条**。⑧ **不建脚本**：静态判定需要后代的 DOM 祖先链，而这在 SiYuan 由模板字符串拼装，无法可靠还原；运行时判定已零假阳性且只需一次 `evaluate`，按脚本准入标准更适合放进验证闭环 |
| 2026-09-14 | **第二十六轮（补记）：聚焦临时展开机制定向审计（已提 #19498）**：① 范围是 `f91cf6b9ed` + `60fa553984` 引入的前端纯视图态机制（`protyle/util/focusFold.ts`、`viewFold.ts` 的 loader、`heading.ts` 的 `getFocusedHeadingChildren`、`blockFold.ts`/`onGet.ts`/`destroy.ts` 接入点）。② **唯一实质发现**：自动展开只认 `NodeListItem`/`NodeHeading`，而仓内两处权威集合都是 5 类（`blockFold.ts:287` 的 `isFoldable`、`gutter/index.ts:3542` 的 `foldRecursive`），折叠的引述块/标注块/超级块聚焦后不展开；定性必须写成「同族覆盖不全」而非回归（原始需求 #6496/#8956/#19483 只提「标题和列表」）。③ **三路取证**：内核 `getDoc` 返回的根元素仍带 `fold="1"` 且子块都在 DOM；把该 DOM 加真实 CSS 产物放进独立页面量高（callout 72px、bq 42px、超级块 38px 是被 `line-clamp:1` 裁掉 336px 内容）；前端守卫与入口可达性。④ **两条取证陷阱已写进层面地图 L6**：`pnpm dev` 只重建 Electron 的 `app` 产物，浏览器用的 `desktop`/`mobile` 不随之更新（在 bundle 里 grep 新符号是产物新鲜度判据）；**不要向运行中的应用发送键盘输入来导航**（本轮实际改坏了用户的一个文档，已还原）。 |
| 2026-09-14 | **第二十七轮：修 `scan_focus_coverage.py` 的双向失准（判据 I3 ⑤）**：① 起因：#19493 的修复 `fa729c7c49` 给 `_reset.scss` 加了一条以 `:is(` 开头的全局兜底，而脚本**两个方向都错**——不展开 `:is()`/`:where()` 就把已被兜底覆盖的控件报成缺口（实测 326 条，其中 `.b3-menu__item`(63)、`.keyboard__action`(38)、`.b3-menu__separator`(35) 等都是假阳性），同时完全看不到运行时赋的类名（#19499 第 2 点就是这类漏报）。② **修法：选择器归一化 + 特异性比较 + 使用点枚举补全**，不做完整级联、不引第三方库——要回答的不是「最终 outline 计算值」而是「聚焦后有没有可见变化」，且元素上的完整类集合是精确已知的，所以嵌套 `:not` 可以**确定性求值**而非猜测。③ **六个判定前提**（前四个是第二十四轮的，五六本轮新增）：`:is()`/`:where()` 展开且**不得进入 `:not()` 内部**（`:not(:is(a, b))` 拆开语义相反）；特异性比较且**分属性**（`:where()` 记 0、`:is()`/`:not()` 取参数最大值；抑制只压得住 outline、压不住 box-shadow，提供者必须**严格大于**抑制者才算覆盖）。④ **实测结果**：使用点 1106 → **1186**（HTML 带 class 1106 / 运行时回推 32 / **无 class 48**），无焦点指示 326 → **2**，且这两条正是 #19499 第 2 点的 `.protyle-toolbar__item`。⑤ **跑到才发现的解析缺陷**：`resolve()` 的 `sel.split(",")` 会把 `:is(a, b, c, d)` 的逗号也切开，选择器被切碎 → 缺口数不降反升到 435；at-rule 的前置部分也不是选择器（会拼出永不匹配的 `X @supports (...) Y`）。⑥ **第三个盲区（交接文件未写）**：旧版要求标签上有 `class=` 属性，**完全不带 class 的表单标签从不进入任何清单**（实测 48 处，`protyle/preview/index.ts:67,70` 即 #19499 的另一半）。⑦ 输出改为四节，后两节明确标注**不计入缺口数**；vendored 的 `asset/pdf/**`（23 处）单列不按自研标准要求。⑧ 自检第 11 组由 14 条扩到 **31 条**，含 `:where()` 计 0 的具体数值、嵌套 `:not` 的正反两向、特异性翻转的三组数据、`createElement` 回推、无 class 收集，外加一条**负向用例**（把展开退化成恒等后 `b3-menu__item` 必须不再被判为已覆盖）。两个自检全绿。 |
| 2026-09-14 | **第二十八轮：三条已上报发现（#19501 / #19502 / #19503）**：① **#19501**：`dev` 顶端前端 CI 是红的（`tests 2468 / fail 2`），两处失败同一根因——`ae226f6ad6`（#19481）新增 `app/src/util/zIndex.ts` 并给 `app/src/dialog/index.ts:2` 加 import，而加载该文件的**两份手写替身表**（`tests/mobileBacklinks.test.js:10-15` 的 `sources`、`electron/connectionManager.test.js:175-182` 的 `modules`）都没同步。与 **#19474 同类**（已由 `dc55a0635` 于 01:26 修复），本次是**约 13 小时后的再次打破**；引入提交自己带了 `util/zIndex.test.ts`（39 行）但没同步替身表。② **#19502**：移动端历史操作筛选下拉（`history/history.ts:538`）的 `fn__size96`（`width:96px`）减去 `.b3-select` 的 `padding: 4px 26px 4px 8px` 后可用文字宽仅 **62px**，**英文 `All operations`（90px）就已超出 28px**（ru 110px、ar 85px、de 84px、zh 27px）；桌面端无 `fn__size96` 因此不受影响（超出 ≤ 0）。③ **#19503**：`emoji/index.ts:983` 的「自定义」标签写死 89px，而 `.fn__flex-center` 只有 `align-self: center`、无 overflow 处理，**de 压入相邻输入框 38px、ru 54px、es 12px、fr 3px、ar 折成两行**；同页签 `language:941` / `date:954` / `format:964` 用同一套 89px，只有译文最长的 `custom` 溢出。**本轮最大的方法教训**：「文本膨胀」候选必须用真实渲染区分「容器太窄」与「译文太长」——两条实测**都是容器侧**（一条对英文就已溢出，一条同族四个标签共用宽度），只按长度比排序会把修法指向错误的侧；且量测要取**文本实际绘制矩形与原相邻控件的交叠量**（观测量），而不是「文本宽-盒宽」（推导量）。另：先量了 `historyOutline` 落在选项集合外，修正后 ar 的数值从 46px 变为 85px。 |
| 2026-09-14 | **第二十九轮：把 a11y 提示位点与剩余候选处理完（#19505 / #19506 / #19507）**：① **#19505（机制层）**：`component/_tooltips.scss` 的提示只有一个显示入口且是鼠标专用——`:70` 基线 `opacity: 0`，`:74-77` 的 `&:focus-within` **只放开 overflow 不设 opacity**，`:79-83` 的 `&:hover::after` 才设 `opacity: 1`。对照实验（真实产物 CSS）：聚焦时 `:focus-within` **命中但 opacity 恒 0**，悬停时 `0 → 1`（轨迹 `0.004(301ms) → 0.686 → 0.935 → 1(451ms)`），对照组（无该类名）`content: none`。规模：160 个 `.b3-tooltips` 标签中**键盘可聚焦 46 个**（28 button + 15 input + 3 a，自研 36）。业务表现最有说服力的是**闪卡复习界面 7 个按钮的 `aria-label` 承载快捷键提示**（`空格/回车`、`1 / j / a`…）——鼠标能看到、Tab 过去看不到，而这些提示正是给键盘用户的。依据 WCAG 1.4.13。**新取证陷阱**：`:after` 有 `transition: opacity 150ms 300ms`，在 `mouseover` 里立即读只能得到过渡中间态（实测误得 `opacity=0`），必须等过渡完成再采样。② **#19506（架构层）**：`dialog/index.ts` 的 `Dialog` 类无任何焦点管理（`:76` 不记录触发元素、`:96-104` 的 `destroy()` 只 remove），`inputDialog.ts` 有初始焦点但不恢复。论据是**同族不一致**：103 个调用点中 **10 个**自己在 `destroyCallback` 里 `focusByRange`、**93 个**没有。浏览器行为对照实验：移除聚焦元素后 `activeElement` 变 `BODY`（第 4 步对照组证明恢复可行）。去重：与 #14399、#12349/#16073/#16862（原生 alert/confirm 整窗失焦）不是同一问题。③ **#19507**：`menus/commonMenuItem.ts:74`/`:144` 的 100px `nowrap` 标签，es 压入日期输入框 **63px**、ru 50px、de 45px、tr 9px、fr 4px（与 #19502/#19503 同形态的第三例）。④ **本轮判为非缺陷（勿重报）**：A2 的 8 处 `aria-hidden` **8/8 全为假阳性**（装饰性 svg/span）；A3 的 1 处 `role="combobox"` **实现完整**（role + aria-expanded + aria-controls + aria-activedescendant + listbox/option）；K6 的 11 处中评分预览**已有完整键盘支持**、菜单 `--current` 是键盘导航的同一状态、其余属辅助信息/鼠标特性 → 三条都写入误报表并给出「判据要看什么」。⑤ 环境事实：运行实例的浏览器页面**反复加载超时**（`http://127.0.0.1:6806` 与 `/stage/build/desktop/index.html` 均 30s 失败），而 `POST /api/system/version` 正常 → 本轮真实渲染取证全部改走「真实产物 CSS + 真实 DOM 形态 + 对照组本地 HTML」。⑥ 未处置：K3/K7 的 123 处焦点陷阱（只处理了「关闭后回焦」一个角度）、判据 I1 状态矩阵仍是空白。 |
| 2026-09-14 | **第三十轮：判据 I1「状态矩阵」人工对照（结论：无实例）**：① 按 `AUDIT-HANDOFF.md` 的要求完成了「先用人工对照拿到真案例再决定是否机械化」这一步，**结论是本仓库当前没有确认实例**，因此**不建脚本、不占判据字母**，改写为层面地图的「状态矩阵检查点（待实证）」——与「资源与生命周期」「定位参照帧」同一处理。② **关键的第一步是「找到四种态的统一实现」**，否则按关键字统计会得到完全错误的矩阵：空态在 `util/Tree.ts:72`（用 `Tree` 的面板自动就有）、加载态**三种并存**（`loading-pure.svg` 44 处 / 图标旋转 `fn__rotate` / `fn__loading`）、错误态在 `util/processMessage.ts:77` + `util/fetch.ts:95`（失败时**回调不执行**，所以「回调里不查 `code`」不是缺陷）、只读态**两种表示**（`config.readonly` 与 `protyle.disabled`，后者由 `protyle/util/onGet.ts:503` 设置）。③ **对照范围**：桌面/移动的 6 对兄弟实现（书签/标签/大纲/文件树/反链/收件箱）+ 26 个菜单文件的只读守卫分布；**逐项排除**六个「看起来像缺陷」的差异（移动端空态=0 来自 Tree；移动端加载态=0 用的是另一种实现；桌面无加载态是平台适配；`menus/block.ts` 的 `transferBlockRef` 由调用点 `!protyle.disabled` 守卫且内核已有 `CheckReadonly`；回调不查 `code` 由 `processMessage` 保证；标签面板两侧加载态实现不同属适配）。④ **本轮第二次踩到「按关键字统计」的坑**：`fn__loading` 判掉 5/6 个面板的加载态、`readonly` 把 `protyle.disabled` 的守卫判成缺失 → **教训：状态的「有没有」必须先确定统一实现落在哪一层（面板 → Tree → 全局工具 → 内核），再问「谁绕过了它」，绕过统一实现才是信号**。⑤ `SKILL.md` 的判据 I1 补上前置步骤与指向检查点的指针，避免下轮重复对照或据此建脚本。 |
| 2026-09-14 | **第三十一轮：对话框焦点陷阱（#19510）+ 六条修复验证 + 工作树落后的重大陷阱**：① **新发现 #19510**：`Dialog` 打开后焦点不进入弹层、也无任何约束——`dialog/index.ts:35-43` 只记录 `previousFocus` 不移动焦点，`:65-66` 模板只有视觉遮罩，`_dialog.scss:24-31` 的遮罩是 40% 透明的全视口覆盖层且点击即关闭（背景对鼠标不可操作，**设计上是模态的**）。当前权威基线上 `event.key === "Tab"` 处理 **0 处**、`focusTrap` **0 处**、`aria-modal` **0 处**、背景 `inert` **0 处**。真实路径复现：打开后焦点留在遮罩后的触发元素，**第 1 次 Tab 仍在背景，第 2 次 Tab 落到可编辑文档区，继续输入把文字写进了对话��框背后的文档**（实测文档内容变为 `WENTBEHIND原始文档内容`）——业务表现是「输入落到错误位置」，不是无障碍细节。② **六条上报全部被修复**（`#19501`/`#19502`/`#19503`/`#19505`/`#19506`/`#19507`，各带提交），并**逐个核对了代码而非评论**；顺带发现 `_content.scss` 与 `_toolbar.scss` 的 `:focus { outline: none }` 两条都被删除 → **#19499 也已修复**。③ **重大方法论陷阱：工作树落后于 `origin/dev`**——本地停在 `bc64d1677b`，dev 已到 `51e0174081`，**落后 9 个提交**（含刚修复的那批）。**`git status` 干净不代表代码是最新的**；判据是 `git merge-base --is-ancestor <sha> HEAD`，而 **`git cat-file -t <sha>` 返回 `commit` 只证明对象被 fetch 过**（我据此误判过一次，与维护者评论相冲突）。用 `git archive origin/dev <paths> \| tar -x` 在**零改动只读仓库**的前提下取得当前基线，重跑扫描器得到权威状态：焦点可见性缺口 **0**（旧树 2）、K2 **0**、A6 **0**、K5「确认」由 18 降到 **16**（剩下的正是已判为容器/内容区的）。④ **K3/K7 的 123 处逐条筛完**：信号极松——38 处是 `b3-menu__item--current`（键盘导航高亮）、15 处 `b3-menu__item--show`（图标显隐），**真正涉及弹层开合的只有 7 处**；**K7（Esc）判定为完备**（`boot/globalEvent/keydown.ts` 的 Escape 按 9 级优先级统一处理，含「对话框高于菜单时先关对话框」与「光标在文档树时回编辑器」）；**K3 的另一半**里菜单不移焦是有意设计（维护者已在 #19493 评论确认）、`block__popover` 是 hover 触发无需移焦。⑤ 误报表 **+3 条**（菜单不移焦是有意设计、弹层开合类名信号过松、Esc 在全局处理器里）；**SKILL.md 验证模式补第 ⑥ 条硬约束**（先确认工作树不落后于 `origin/dev`）；层面地图「环境与取证前提」新增整节说明该陷阱与取得当前基线的方法。⑥ **修正自检里一条会挡住正确状态的断言**：第 9 组的 `0 < 缺口 < 总数` 在缺口被修完后把**正确的 0** 判成失败（与「假成功」相反但同样有害）；拆成「活体检查」（样式侧解析出焦点规则、`有焦点指示 + 无焦点指示 == 总数`、缺口为 0 时必须有大量已覆盖）与「降噪过度检查」（留在第 11 组夹具断言，与仓库状态无关）。**负向验证 3/3**：CSS 解析失效 / 使用点收集失效 / `verdict` 恒真三种变异都被抓住；其中第二种由同组的另一条断言拦下，印证维护规范第 13 条「拦住要放宽到该组任一相关断言失败，不要锚定单一标签」。 |
| 2026-09-15 | **第三十二轮：块选择模式借用原子标记导致残留与方向键跳块（已提 #19556）**：① 起点是用户对 `protyle-wysiwyg--navigation` 语义的追问，范围定为纵向导航 × 块选择的交叉。② **在建立端**：`focusAtomicRegion`（`verticalNavigation.ts:94-113`）的三条分支**最后都会**加该类（包括 `focusBlock` 成功的普通可编辑块分支），而 `keydown.ts:531`/`:566` 让块选择模式每次 ↑/↓ 都调用它（含折叠块/自定义块在内的“安全聚焦”副作用）→ 普通段落被当成原子位置。③ **在清理端**：退出模式只清 `--select-mode`/`--select`（`blockSelection.ts:49`），全仓无一处清理 `--navigation`——而同族 `--select`/`--select-mode`/`--hl` 各在 2–3 个载体里被清，“类名 × 清理载体”矩阵只留一个零覆盖项。④ **可观测后果**：退出后残留 2px 轮廓，且下一次同向方向键走原子分支（`keydown.ts:1008-1020`）**跳过当前多行块的其余行**。⑤ **真实渲染 + 真实键盘差分**：残留路径 ↓ 从 B 直接跳到 C；对照（手工移除该类、光标复位）真实 ↓ 连续 4 次使光标顶部 244→270→296→322（每步一个行高 26px）——两边起点完全一致，唯一差别就是那个类。**还逐个退出路径验了**：Esc 与 Enter 残留、鼠标点击不残留（`pointerdown` 捕获监听）。⑥ **一个负面结论也取了证**：临时类确实随载荷发往内核（挂钩 `fetch` 捕获到），但内核丢弃 block DOM 的 `class`，读回 `.sy` 不含它 → 降为观察项，并写入误报表。⑦ **判据/模式**：新增 **D3e** 与 **P42**（借用型临时标记类无人释放）；误报表 **+2 条**。⑧ 取证环境：直接加载 `/stage/build/desktop/`（被服务端判为 Electron UA 时会送 `/stage/build/app/`，其 `require("electron")` 在无 nodeIntegration 的页面直接崩成错误页）；删除文档要用 `/api/filetree/removeDocByID`。 |
| 2026-09-15 | **第三十三轮：块选择模式的第二个入口缺状态位（已提 #19557）**：① 起点是用户质疑上轮范围太窄——上轮是**定向审计**，只回答了提问所处的那个交叉点，于是本轮把同一功能域的其余路径补齐。② **变体分析先于取证**：`grep` 「移动当前块」的全部调用点（`setBlockSelectionModeElement` 11 处）与「移动导航标记」的全部调用点（`focusVerticalBlockSelection` 2 处）取差集，再逐条实测区分**自愈**与**残留**。③ **唯一实质发现**：块选择模式有两个入口，状态集合不等价——光标入口只建「当前块」（`--select-mode`），划选转换入口只建「标记块」（`selectBlocksByRange`，`util/selection.ts:167`），各只有一半。真实鼠标划选 + 真实键盘实测：**划选转换态按空格 → 标记全清且 P3 正文由 `P3short` 变为 `P3short `**（空格被写入文档）；按 ↓ / Enter 也是先清空全部标记。**对照组**（同一编辑器、同一按键、只改进入方式）：光标入口下空格标记成功且**逐块比对文档未被改动**。④ **根因是分派链上的退化**：空格专用分支要求 `blockSelectionModeElement`（`keydown.ts:571`），状态位缺失时**不停下**，继续落到 `isRegularTextInput`（`:594-596`，只排除「有当前块」而**没排除「有标记块」**）→ `hideElements(["select"])`（`:618`）清标记 → 字符写入正文。⑤ **新洞察写入判据 I1 的第二个维度**：状态集合要**按入口展开**比对；且**缺失状态位导致的是「退化」而非「无操作」**，后果可能从「没反应」变成「写入数据」，因此必须沿分派链追到实际执行的分支。⑥ **排除的候选（勿重报）**：`insertEmptyBlock` 对 `undefined` 有兜底（`block/util.ts:334-350`），A/B 不崩溃；`⇧↓` / PageUp/PageDown / Tab / 鼠标退出四条路径都**不残留**，但原因各异（光标真移动使 keyup 的 `clearStaleAtomicFocus` 自愈 / DOM 被替换 / `pointerdown` 监听），**规律是「光标是否真的移动」而不是「代码里有没有调移动函数」**——已写成误报表新条目。⑦ 误报表 **+1 条**（静态推演出的同族残留候选）；同时把 I1 的「四种态无实例」结论**限定回空/加载/错误/只读**，避免被本轮的实例误读。 |
